# 2026-06-12 Session — idea sweep + regen-controls design (no code)

## Goal
Brush off old ideas (broad sweep of legacy-prospecting, resolutions §4, upstream issues),
pick a target, and `/spec` it.

## What landed
- **Idea sweep:** unrouted finds from old team issues — quick-add starter catalog (#101,
  candidate W-track "Move D"), wardrobe gap detection (#66, possible M6 sidequest),
  style-from-picture (#66, fold into B-track B2). Not yet recorded in any plan — surface
  them when the W-track / B-track `/spec` sessions happen.
- **R9** in `spec-resolutions.md` — regen controls resolved: locks + contextual dislikes as
  per-request Step-4 filters; hybrid escalation (one constrained Steps-1–3 re-entry on
  starvation, locks pinned pre-sampling, merge into session pool deduped by FullSignature);
  partial + explicit notice on failure; `changeTarget` + `feedbackNotes` dropped.
- **`docs/plans/regen-controls.md`** (new) — execution detail for M3 (pure functions + pytest
  list) and M5 (single-route wiring, regenerate route deletion, modal edit).
- **`legacy-prospecting.md`** — §3.1, §5 M3 row, F14 row repointed at R9 (conflict scrub).

## Decisions / why
All via interview (recorded in R9 / the plan): hybrid escalation over filter-only (locks
starve a 40-candidate pool) and over always-regenerate (cache win lost); merge-into-pool over
lock-keyed cache entries (one entry per session, repeat locked re-rolls free); drop
changeTarget (locks express it) and feedbackNotes (UI never sends it on regen; labels persist
via interactions).

## Blockers / open questions
None. R9 build work is owned by M3/M5; nothing changes for M0/M1.

## Next session: start here
**M0-3 (keys)** per `docs/plans/m0-m1-substrate.md` §3 — unchanged from the 2026-06-11 note
(then M0-4 normalizer fresh, M0-5 seed; F7 → M0-5 `str()`-ify int seed fields). Key functions
are Python-only per the key-locus decision (resolutions §4).
