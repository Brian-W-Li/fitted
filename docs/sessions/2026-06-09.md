# 2026-06-09 Session

## Goal
Revive fitted from the dormant CS 148 team repo. Set up a personal fork and bootstrap a Claude Code workspace.

## What landed
- **Fork created** at `github.com/Brian-W-Li/fitted` (public, MIT). Bare-clone + mirror push, so no "Forked from" badge but original commit attribution preserved.
- **Working clone** at `~/Documents/fitted/` with `origin` → fork, `upstream` → `ucsb-cs148-w26/pj12-outfit-recommender`.
- **`CLAUDE.md`** at repo root — project overview, run commands, env vars, where the recommendation flow lives, current focus, out-of-scope, conventions.
- **`.claude/settings.json`** — pre-approves common dev commands (npm install/test/dev/build/lint, python3 under ml-system/, git read-only ops); explicit deny on `git push --force`, `reset --hard`, `clean -fd`.
- **`docs/sessions/`** convention — README + this log.
- **`.claude/commands/sync-upstream.md`** — `/sync-upstream` slash command to pull from team upstream.

## Decisions / why
- **Scope: portfolio + technical depth, not public launch.** Teammates have talked launch for two quarters; Brian privately thinks that's wrong fit for a 4–8 hr/wk solo cadence. Portfolio-piece framing is honest and shippable.
- **Technical-depth dive: `ml-system/`.** It's an orphan (Issue #32) — built standalone, never wired into the deployed app. Clean interface (`_score_outfit` returns a number), team-written spec in `mlWhatWeAreGoingTodo`, and the deployed app keeps working independently. Replacement target: a feedback-trained shortlister that pre-filters wardrobe before the LLM sees it (issues #84, #112).
- **Visual try-on (#82, #87–92) is v2.** Higher demo wow but diffusion-model territory, less aligned with systems strength.
- **Fork via bare-clone + mirror push, not GitHub's Fork button.** Keeps full history without the "Forked from" badge — better signal for portfolio use.

## Blockers / open questions
- **`CV_SERVICE_URL`** in env defaults to a teammate's Hugging Face Space (`theanimated01-fitted-cv.hf.space`). Brittle long-term; flag if it 404s.
- **`.env.local` not in this clone.** Pull from previous CS 148 work or regenerate from Firebase/Mongo consoles.
- **No first commit on the fork yet.** `CLAUDE.md` and the `.claude/` setup are uncommitted. Commit before next session.

## Next session: start here
1. Commit and push the setup: `git add CLAUDE.md .claude/ docs/sessions/ && git commit -m "add CLAUDE.md, settings, and sessions convention for fork" && git push`.
2. Restore `fitted/.env.local` from your old CS 148 work; verify with `cd fitted && npm install && npm run dev` (should serve on `localhost:3000`).
3. Start the ml-system arc: open `docs/plans/shortlister-v1.md` (create it) and write the v1 plan — first concrete step is wiring `ml-system/outfit_recommender.py` to read real `OutfitInteraction` data from Mongo instead of the hardcoded demo wardrobe.
