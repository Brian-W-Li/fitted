# H26 C4 — full audit (core + RUN tooling) + Step-1 (B2) completion — 2026-06-30

Session ran `docs/sessions/h26-c4-audit-prompt.md` (the exhaustive ultracode audit of ALL of C4 — the
judge/emission core AND the RUN-phase operator tooling). Two things landed: (1) the audit converged with
fixes; (2) **Step 1 / B2 (the embedding-cache → `selection.json` build) finished on Brian's terminal mid-session.**

## Baseline
`.venv/bin/python -m pytest` from `ml-system/experiments/h26/`: **208 passed / 2 skipped** at start; ruff clean.
Floor grew to **219 passed / 1 skipped** by end (see counts note below). Nothing committed — Brian drives git.

## Audit — heavy multi-lane loop (ultracode workflows), verify-before-acting on every finding
**Round 1** (9 lanes → adversarial verify): **12 confirmed → 9 distinct load-bearing findings, all fixed**
(2 of them my own catch-net deep-read). Most-severe first:
- **D1 (`.gitignore`, was blocker→graded important):** five RUN-scratch patterns carried inline `#` comments →
  git treats the whole line as a literal pattern → `closet_input.json` + `calibration_viewer.html` +
  `calibration_answers.json` + `calibration_questions.json` + `calibration_pilot.ndjson` were **NOT ignored**
  (empirically confirmed via `git check-ignore`). Fixed: comments on their own lines (+ added
  `calibration_answers_recheck.json`). Not a pure privacy blocker (`closet_input.json` ⊆ the intentionally-
  committed `closet_manifest.json`; photos double-guarded), but a real leak-surface/repo-bloat defect.
- **D2/D3/D5 (`run_judge.py` gate-b build-order teeth):** `cmd_gate_b` scored held-out TEST questions with only
  a `k_samples`-is-an-int check (not committed-frozen), and used a **hardcoded default snapshot** (not the frozen
  addendum's), and never drift-checked `fitb_order.json` before spending. Fixed: new `require_frozen_envelope()`
  (schema-valid `frozen:true` + committed-clean, mirroring emit's teeth) — replaces `_frozen_k`; gate-b reads the
  **entire** envelope (K/snapshot/max_tokens/retry_budget) from the frozen addendum (no CLI overrides on gate-b);
  `verify_fitb_order` before `run_arm`; and `evaluate.assert_ledger_snapshot` binds every consumed ledger row's
  `model_snapshot` to the frozen envelope (`compute_gate_b(expected_snapshot=…)`, wired through `materialize`).
- **D4 (`run_judge` had zero hermetic coverage):** extracted the blindness-critical pilot/gate-b scoring into pure
  `pilot_summary`/`gate_b_summary` helpers + added tests pinning the **human-vs-Polyvore split** (the §F forbidden
  path), the freeze-gate refusals, and the ledger-snapshot binding.
- **D6 (`intra_annotator_agreement` schema-required but hand-filled):** added `make_calibration.restability()` (a
  real re-label agreement measurement) + documented it in the recipe.
- **D7 (`assemble_closet` auto-defaulted a *passing* label audit):** now requires an explicit `label_audit` block.
- **D8/D9/F-C (docs):** run-tooling recipe reconciled (emit blocked until B3; move `calibration_answers.json` into
  the h26 dir; restability step); README + build-doc stale test counts fixed.

**Round 2** (9 lanes; **truncated by a session usage limit** — `data-integrity` lane + 2 verifiers errored). 7
lanes returned clean; 2 raw findings surfaced, both **unverified by the workflow** so I verified them against
source myself and completed the missing lane manually:
- test-quality "cmd handlers untestable" → **minor**: the freeze guard *is* tested in isolation; only the handler
  *ordering* was unpinned. Fixed with `test_cmd_gate_b_refuses_scaffold_before_any_spend` (drives the real handler).
- operational "freeze checklist is a subset of schema fields" → **minor** (doc clarity): the committed scaffold
  already carries **every** schema-required field (verified programmatically), so the risk is only if the operator
  authors fresh. Fixed by clarifying the recipe to "edit the committed scaffold in place."
- `data-integrity` (self-run): no `.gitignore` control-pattern regression; cache universe complete; seeds distinct;
  no silent truncation — clean.

**Round 3** (focused: blindness/privacy/correctness/spec-fidelity/data-integrity, Step-1-aware) — see the commit /
memory for the outcome.

## Step 1 (B2) COMPLETED mid-session — the trigger Brian asked to be told about
`build_cache_and_select.py` finished on Brian's terminal (~20:12 cache, ~20:28 train). Now present:
`embeddings/embeddings_fashionsiglip.npy` (255 MB, gitignored), `fashionsiglip_ids.json`,
`fashionsiglip_hashes.json`, `checkpoints/{pairwise_edge_grid_0,item_level_grid_1}_seed20260629.pt`, and
**`selection.json`**. `embedding_manifest_fashionsiglip.json`'s cache-content fields are populated
(`n_items=83178` + the three sha256s).

**`selection.json` verified (the assistant's job):** schema-valid; `manifest_hashes` bind the current frozen
artifacts; **carries no metric value** — only training hyperparameters + `converged:false`/`early_stop_epoch:48`
(blindness §1 intact; `checkpoint_sha256=a172be27…`). The `test_embedding_freeze_agrees` failure that appeared was
the **legitimate C3 cache population**, not a regression — reconciled the test to accept both the cache-deferred
(all-null) and cache-built (all-populated + well-formed sha256) states and fail on a partial population.

**NOT done (Brian's calls):** the full B2 finalization — the *deterministic checkpoint-sha re-derivation*
(re-run `train_head` on the built cache, compare to `selection.checkpoint_sha256`) and **committing**
`selection.json` + the populated `embedding_manifest_fashionsiglip.json` (recipe says commit both). Then **Step 2**
(calibration picks — needs Brian's human labeling in the viewer, then `restability`), Step 3 deferred, Step 4.

## Test-count note (a floor, not a pin)
219 green / 1 skip reflects the **cache-built** tree (the `selection.json` drift guard un-skips once
`selection.json` exists; the remaining skip is the opt-in live-judge smoke). A fresh checkout with the cache
deferred reads 218 / 2. Counts grow with B2 state; don't treat as a pin.

## Cost
~$0 in live spend (fully static + hermetic + adversarial reasoning; no OpenAI calls, no cache re-build).
Workflow subagent tokens: R1 ≈ 2.5M, R2 ≈ 1.6M (truncated), R3/R4/R5 focused (R5 killed by Brian as
tail-overkill — the heavy fan-out earns its cost early, a single reviewer is right at convergence).

## B2-result audit (the code Brian RAN + its output — done at session close)
The C4 audit was static, so it validated `build_cache_and_select.py` as *code* but never its *result*. Ran
that now against the actual on-disk artifacts:
- **Cache:** 83,178 × 768 float32, L2-normalized (1.0000); the manifest's content-hash (`matrix.tobytes()`,
  numpy-version-independent by design) + ids-hash bind the real files; `embed.load_cache()` accepts it
  fail-loud; backbone revision `c56244cc` — no drift. *(A first pass flagged a sha "mismatch" — it was me
  hashing the `.npy` file vs the array-content hash the manifest deliberately uses; ran it down, clean.)*
- **`selection.json`:** winner `grid_0`, schema-valid, binds the frozen artifacts, **no metric value**.
- **CAUTION (not a bug):** `converged:false`, best epoch 48/50 — the winning head was **still improving on
  valid when it hit the frozen 50-epoch budget** (possibly budget-limited). The spec surfaces this via the
  `converged` flag deliberately (blindness — the valid curve stays sealed). If the head later misses **gate
  D**, the budget is the first suspect (frozen at C2 → bumping it is a pre-registration decision, not a tweak).
- **NOT yet run:** the deterministic checkpoint-sha re-derivation (re-run `train_head`, confirm it reproduces
  `a172be27…` — local, no API, but re-trains the grid). The one remaining B2 sign-off.

## Session close — status + continue C4 tomorrow
**Where we are:** C4 CODE + RUN tooling built + audited clean (5 rounds), 221 pytest green / 1 skip, ruff
clean. B2 done + verified. `metrics.json` NOT emitted (correct). **C4 as a whole (the RUN *result*) is NOT
finished** — blocked on B1 (key), the judge freeze, and B3 (closet). The last H26 checkpoint is **C6**
(report + mechanical A∧B∧D verdict); C4 → C5 (closet domain-gap) → C6 remain.

**Continue C4 tomorrow (the RUN phase):**
1. *(optional)* deterministic checkpoint-sha re-derivation to fully sign off B2.
2. **Step 2** — calibration picks in the viewer → `restability` → commit `calibration_set.json`.
3. **B1** → `run_judge pilot` → **FREEZE `judge_addendum.md`** (blind, fill only the per-run fields) → commit → `gate-b`.
4. **B3** (closet) → `assemble_closet` → commit `closet_manifest.json`.
5. `run_judge emit` → `metrics.json` → **C5** (domain probe) → **C6** (report + verdict).
6. **When C4 actually finishes (metrics.json emitted):** the full big-picture audit Brian wants —
   correctness + spec-fidelity + ambition-merit, v2 spec as the source of truth.

**Commits landed this session (grouped — the cache + checkpoints stay gitignored, regenerable from the seed):**
1. **C4 judge + emission core** — `gpt_judge.py`, `evaluate.py` (emission half + audit hardening),
   `judge_addendum.schema.json` + scaffold, core tests, `.gitignore`.
2. **C4 RUN tooling** — `build_cache_and_select.py`, `live_content.py`, `make_calibration.py`,
   `run_judge.py`, `assemble_closet.py`, `closet_input.template.json`, `test_run_tooling.py`.
3. **B2 sealed artifacts** — `selection.json` + the populated `embedding_manifest_fashionsiglip.json`.
4. **Docs** — build-doc §15-C4 + counts, spec §20/§23 status, README, session notes + audit/kickoff prompts.
