# 2026-06-26 — M4a close

## Goal
Land M4a **C3** (PreferenceSummary rip), heavy-loop audit the **M4a boundary**, and stand up a
self-owned local dev stack to verify the data path live.

## What landed
- **M4a is complete (C1–C3), boundary-hardened, and live-verified.** The data path — 5-value
  `clothingType` + keyword-derived `warmth` — is correct end-to-end against a real Mongo (seeded a
  test wardrobe through the real derivation: "Dress Shoes" → `shoes`, warmth 0–10 across the spread).
- **C3 — PreferenceSummary ripped wholesale.** Deleted `models/PreferenceSummary.ts`,
  `lib/runPersonalizationSummary.ts`, `app/api/preferences/summarize/`, the account "Style profile"
  card, the dashboard background poll, `getOrRefreshPreferenceSummary` + its calls in
  recommend/regenerate, and 2 jest suites. `gemini.ts` keeps only `inferWhyForInteraction`. Grep-clean.
- **Boundary-audit fixes (3 rounds + dual-read design review, Fable unavailable):**
  - `lib/clothingType.ts` — "Dress Shoes" no longer mis-stored as a one-piece `dress`. "dress" counts
    as a one-piece only when it's the **head noun**, not an adjectival modifier; the modifier noun-set
    is **derived from the rung arrays** (`SHOE/BOTTOM/OUTER_KEYWORDS`) so it can't drift, guarded by a
    drift-guard test. Added `sundress`/`gown`/`frock`. **Do not revert toward category-authoritative or
    cascade-reorder — both regress real one-pieces ("shirt dress"→top).**
  - `lib/wipeGuard.ts` — authorize on **host only**; a `fitted-dev`-named db on the shared team Atlas
    host could previously self-authorize the wipe. `wipe-db.ts` gate (c) confirms the actually-connected
    `db.databaseName`.
  - New `tests/wardrobeEditIngestion.test.ts` — the §14.2 C2 edit round-trip guard (was missing).
  - Docs reconciled: plan §10.3 (adjectival trap-guard) + §14.2 (wipe host-not-dbname) + spec §23 H37.
- **Self-owned dev stack (zero teammate deps):** `docker-compose.yml` → local `mongo:8` at
  `mongodb://localhost:27017/fitted-dev` (reproducible, no Homebrew/Xcode). `scripts/seed-test-wardrobe.ts`
  re-seeds via the real derivation. Brian's **own Firebase project `fitted-f7372`** + his **own OpenAI
  key** in `fitted/.env.local` (gitignored). `CV_SERVICE_URL` blanked (teammate HF Space removed).
- 275 jest green; app compiles; Python suites untouched.

## Next session: start here
- **C4** — M4b's first checkpoint (dormant Python substrate). Add `fitted_core.__version__` /
  `promptVersion` / `rankerConfigVersion` (`__init__.py`/`config.py`) + new
  `ml-system/fitted_core/snapshot_serde.py` (snake↔camel field maps for `engineVisible`, `to_wire`/
  `from_wire` enforcing finite-floats-only + opaque-string ids). Spec at
  `docs/plans/m4-data-model-migration.md` §14.2 **C4**. Pure additive Python; nothing calls it until M5.

## Follow-ups
- `.env.sample` still lists theanimated01's CV URL as the template default and `CLAUDE.md` documents it —
  scrub both for full teammate-dep removal (offered, not done). Small.
- **W-track owns:** the real CV (his own, replacing the HF Space), the photo/image pipeline, and
  re-deriving `clothingType`/`warmth` on an item **edit** that changes category (PATCH doesn't today; C2
  acceptance met regardless — §18).
- **Deploy (M5):** his own Firebase is wired for dev; his own **Atlas** cluster is needed for deploy
  (Vercel can't reach localhost) — a one-line `MONGODB_URI` switch when the time comes.
