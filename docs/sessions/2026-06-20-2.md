# 2026-06-20 Session (2) — M3 C4

## Goal
Land M3 **C4 (Step-6 diversity)** on the ranker, harden its mutation coverage before C5, and leave a
clean handoff. (Earlier same-day artifact `2026-06-20-m3-ledger.md` is the frozen decision ledger, not a
session note.)

## What landed
- **C4 diversity** in `ml-system/fitted_core/ranker.py` (commit `3533b6b5`, pushed):
  - `_apply_variant_cap` — keeps top-`BASEKEY_VARIANT_CAP`(2) per BaseKey by **Step-5 pre-penalty**
    score, canonical `(-score, full_signature)` order, **never `source_index`** (N5/N6).
  - `_compute_overuse_set` — computed **once** over post-variant-cap survivors; **strict** gate
    `> OVERUSE_MIN_POOL`(15) and **strict** threshold `> OVERUSE_THRESHOLD`(0.40); frequency counts
    every filled slot incl. optional outer/shoes (N1/N2/Q1/N13).
  - `_rescore_with_diversity` — signed `overuse` (−0.5 per overused filled item) + flat `repetition`
    (−1.0 once on a shown FullSignature, recency-invariant); preserves C3 terms incl. `cooldown`;
    `score == Σ breakdown` holds (Q1/Q2/N4).
  - `_apply_step6_diversity` — composes the three; **no** sort / truncate / fallback / re-admit /
    assembly (those are C5). `rank()` still raises `NotImplementedError` on non-empty input by design.
  - 21 C4 tests added.
- **Mutation-coverage patch** (tests-only; commit `95c1a8e8`, pushed): 4 focused tests closing live
  mutants —
  1. lock+cooldown double-failure must **not** enter the cooldown-relax reserve (N3),
  2. variant cap keys on **Step-5** score, not post-diversity score (N5),
  3. `_compute_overuse_set` counts **optional outer/shoes** in its frequency denominator (N13),
  4. C4 diversity **does not globally sort** (C5 owns final ordering).
  Each test was verified to **fail under its injected mutant**, then the mutant reverted —
  `ranker.py` ended unchanged.
- Test count: **446 → 471 passed**.

## Documentation consistency (this closeout)
- `CLAUDE.md` routing line de-staled — was "M3 ranker, at C1 — implementation not yet started"; now
  points at the plan's checkpoint table (drift-resistant) and names the next open checkpoint.
- `docs/plans/m3-ranker.md` checkpoint table — C0–C4 marked complete; **C5 is the next checkpoint.**
- `docs/Fitted_Spec_v2.md` §20 prose — "M3 (the ranker) is next" → "in progress" (milestone-level;
  the `[NOW]` rung is unchanged). Resolves plan §15 seam #1.
- `docs/README.md` and `ml-system/README.md` routing — now point at active M3 status without claiming
  M3 is complete.

## Verification
- `ml-system/.venv/bin/python -m pytest -q` → **471 passed**.
- Code/test baseline at `95c1a8e8` before this docs closeout; working tree otherwise clean.
- `RECOVERY.md` already in its clean template state — no recovery scratch to fold in.

## Next Session: Start Here
1. **M3 C5 — fallback + tie-break + assembly** (plan §6 steps 6–8, §11):
   - fallback ladder `none → overuse_relaxed → variant_cap_relaxed → cooldown_relaxed → insufficient`
     (overuse-relax drops the penalty, never recomputes the set — N2; locks/contextual dislikes never
     relax — N3);
   - sort by score desc + **greedy tie-break** (canonical `full_signature` pre-order → least-represented
     BaseKey so far → `tiebreak_seed(…, generation_index)` seeded priority — N6/N12);
   - truncate to `k`, assemble `RankedOutfit`s with defensive `SlotMap`/`changed_item_ids` snapshots,
     derive `relaxed_cooldown_count` from the **emitted** set; **wire `rank()` to return** (remove the
     `NotImplementedError`).
   - **Run the Spec Conformance Audit Gate first** (plan §11). C5 is the determinism-critical checkpoint;
     suggest splitting **C5a** (ladder + flags) / **C5b** (sort/tie-break/assembly) as separate
     green-test commits.
2. **M3 C6 — closeout**: §12 mutant pass; flip `ml-system/fitted_core/__init__.py` + `ml-system/README.md`
   status "M0–M2" → "M0–M3"; add the `> COMPLETED` banner to the plan.
