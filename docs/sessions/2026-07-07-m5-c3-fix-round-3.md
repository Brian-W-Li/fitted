# 2026-07-07 Session

## Goal
Close the remaining M5 C3 correctness findings after `0854abcb`, then run the owed C1/C2/C3 + spec
absence-shaped audit lanes before committing.

## What landed
- C3 service failures after validation now degrade to corpus rows across the full post-validation span:
  reducer/scorer/generator-construction raises produce `stage="pre_generation"` no-attempt payloads, and
  assembly failures produce `stage="assemble"` payloads with best-effort trace salvage.
- Assembly salvage now keeps the engine-visible pool and trace diagnostics when recoverable; the
  last-resort assemble shape with empty attempts is explicitly valid when attempt salvage itself failed.
- `M5_MAX_COMPLETION_TOKENS` now has both a hard ceiling and readiness floor in `service/config.py`;
  `/readyz` rejects values outside the band.
- `m5-cutover.md`, `Fitted_Spec_v2.md`, and `snapshot.py` comments all describe the three failure loci
  consistently: attempt-only, no-attempt pre-generation, and assemble-with-best-effort-salvage.
- Verification: `git diff --check`; `cd ml-system && .venv/bin/python -m pytest tests service/tests`
  (923 passed); `cd ml-system && experiments/h26/.venv/bin/python -m ruff check service fitted_core`;
  `cd fitted && npm test -- --runInBand` (387 passed).

## Next session: start here
Begin C4 only after reading `docs/plans/m5-cutover.md` C4 and the C3 landed block around the C5 schema
trap-guards. The important next traps are `controls` activation, scoreTrace/scorer-seam population, and
the C5 strict-Mongoose read-back requirements.

## Follow-ups
- The pre-C5 empirical cap gate is still owed before any live write: run daily + rescue against real
  `gpt-5.4-mini`, then re-tune `DEFAULT_MAX_COMPLETION_TOKENS`, `MIN_COMPLETION_TOKENS_FLOOR`, and
  `DAILY_MAX_CANDIDATES` together if needed.
- C5 must add the GenerationSnapshot schema additions and read-back tests for `engineFailure`,
  `finishStatus`, generator provenance, requestId, controls, and the assemble shape.
