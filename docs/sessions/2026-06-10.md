# 2026-06-10 Session 1

## Goal
End-of-day, deliberately small: scope the day and start M0 *carefully*, anchored to the
already-verified plan (`docs/plans/m0-m1-substrate.md`). Land **M0-1 only** (config constants +
`fitted_core/` scaffold + pytest), leaving the dense M0-4 normalizer for a fresh head.

## What landed
- **M0-1 complete.** Created the `ml-system/fitted_core/` package and `ml-system/tests/`:
  - `fitted_core/config.py` — every named constant M0/M1 reference, values pinned to plan §1.5
    (`DEFAULT_K=10`, caps `35/30/25/20/25`, `MAX_PROMPT_ITEMS=135`, `MAX_CANDIDATES=40`,
    `RANDOM_FRACTION=0.7`, `MIN_SIGNAL_THRESHOLD=5`, forward-declared `MAX_AFFINITY=20`/
    `OVERUSE_MIN_POOL=15` with `# used in M3`). Verified caps sum 35+30+25+20+25 = 135 = `MAX_PROMPT_ITEMS`.
  - `tests/test_config.py` — 3 tests, all asserts the plan's M0-1 task names (ceilings, caps-sum
    regression guard, sampler constants). **3 passed.**
  - `tests/conftest.py` — placeholder; the demo-wardrobe fixture lands in M0-2 (needs `WardrobeItem`).
  - `requirements.txt` — added `pytest>=7.4`.
  - `ml-system/.gitignore` — Python cruft (`__pycache__/`, `*.py[cod]`, `.pytest_cache/`, `.venv/`).
- Run command confirmed: from `ml-system/`, `python3 -m pytest` (rootdir = `ml-system/`,
  `fitted_core` importable via the package layout — no extra pytest config needed).

## Decisions / why
- **M0-1 only, by design.** Brian flagged low energy ("end of a long day") and asked to "start
  carefully" off the verified plan. M0-4 (SlotMap normalizer + error channel) is the load-bearing
  correctness task the Fable review caught bugs in — explicitly deferred to a fresh session.
- **Conservative comments.** For forward-declared constants (appendix A3/B1/B2) the config comments
  cite the appendix tag only, not invented behavior descriptions — staying source-grounded since
  those appendices weren't re-read this session.

## Blockers / open questions
None. Setup is uncommitted (Brian drives commits; offered).

## Next session: start here
Proceed in plan order: **M0-2 (data model)** → M0-3 (keys) → **M0-4 (normalizer, the dense one —
do this fresh)** → M0-5 (seed). M0-2 also fills in `conftest.py`'s demo-wardrobe fixture
(re-express the legacy `outfit_recommender.py` wardrobe in the new 5-type schema; do **not**
migrate in place — plan §1.5 decision 2). Two contracts to honor when you reach them: M0-4's
`normalize_to_slotmap -> tuple[SlotMap | None, reason]` error channel, and M0-5's length-prefix
seed encoding `f"{len(s)}:{s}"` + typed `None` date sentinel.

---

# 2026-06-10 Session 2 — M0-1 verification pass

## Goal
Source-grounded verification of M0-1 before continuing: confirm config values against the PDF
itself (not the plan's restatement), check tests match the M0-1 task, and read ahead through
M1-5 for downstream collisions. Weight determinism (cold-start is the only prod path pre-M6).

## Verified (all source-grounded)
- **`python3 -m pytest` from `ml-system/`: 3 passed.** test_config covers ceilings, the caps-sum
  regression guard, and sampler constants — matches the plan's M0-1 test spec exactly.
- **Constant VALUES vs the PDF (extracted via `pdftotext -layout`): zero transcription drift.**
  Confirmed each against the spec text, not the plan: `DEFAULT_K=10` (§ "default K = 10"),
  caps `tops35/bottoms30/dresses25/outer_layers20/shoes25` (§7.2 table), `MAX_PROMPT_ITEMS=135`
  (§7.2/§18), `MAX_CANDIDATES=40` (§7.4/§18), 70/30 (§7.3), `MIN_SIGNAL_THRESHOLD=5` (B2),
  `MAX_AFFINITY=20` (A3), `OVERUSE_MIN_POOL=15` (B1). Caps sum 35+30+25+20+25 = 135. ✓
- **Forward-declared set is correct for scope.** The spec's other named constants
  (`COOLDOWN_PENALTY`, `OVERUSE_THRESHOLD`, `OVERUSE_PENALTY`, `ITEM_BOOST_WEIGHT`,
  `DISLIKE_PENALTY`, `COMBO_BOOST`, `BASE_SCORE`) are all M3 scoring/ranking — correctly absent
  from M0-1; config.py grows in M3. Only A3/B1 forward-declared, as planned.
- **File layout matches plan §2** for the M0-1 slice (config.py, test_config.py, conftest
  placeholder, both `__init__.py`, requirements pytest pin, .gitignore). models/keys/slotmap/
  seed/sampler correctly absent (M0-2+).

## Findings
- **F1 (decision-changing, Fable-reviewed twice → R6).** `RANDOM_FRACTION = 0.7` is a float trap:
  the resolved integer half-up split `(cap*7+5)//10` (M1-3#4 / R4) never uses it, and the obvious
  `round(cap*RANDOM_FRACTION)` reintroduces the banker's-rounding bug that breaks cross-reimpl
  determinism on the only prod path. First Fable read said "integer constants + helper"; Brian
  pushed back (belt-and-suspenders), second Fable read settled it: **helper-only**. → **R6**:
  delete `RANDOM_FRACTION`, add **no** constants; the split is the sampler-owned `random_count(cap)`
  helper (`7`/`10` local), §18 ruled inapplicable (structural, not a tunable knob). Drift guard is
  the value table (35→25, 30→21, 25→18, 20→14). **config.py deletion PENDING Brian's go**; the
  helper itself lands with M1-3.
- **F2 (doc fix, applied).** spec-resolutions R1's code block still said `join with "\x1f"`,
  contradicting R1's own superseding length-prefix prose — a landmine for M0-5 (next). Corrected
  the comment to point at the length-prefix encoding note.
- **F3 (test gap, APPLIED).** The caps-sum guard passes under a compensating drift (e.g. 36/29/…
  still = 135) that would change M1-3's per-type split. Added `test_per_type_cap_values` pinning
  each cap (35/30/25/20/25). pytest now **4 passed**.
- **F4 (§18 gap, DEFERRED to M1-4 — reminder only).** §7.4's `x 3` multiplier and `<= 5` threshold
  are inline magic numbers; §18 mandates named constants. Add `CANDIDATE_MULTIPLIER=3`,
  `CANDIDATE_SCALING_THRESHOLD=5` **when M1-4 lands** (not now — adding constants ahead of their
  use invites guessing at meaning).
- **F5 (layering note for M1).** Caps are 5 scalars; M1 needs an `ItemType→cap` map. Build it in
  `sampler.py` (imports both models+config), NOT in config.py — config is the lowest layer and
  importing `ItemType` would risk a cycle. Naming is irregular but unambiguous (`CAP_OUTER` ↔
  `ItemType.outer_layer`, `CAP_TOPS` ↔ `ItemType.top`).
- **F6 (cosmetic).** `DEFAULT_K` is unused until M3 (ranking/fallback "return top K") but isn't
  labeled `# used in M3` like the other forward-decls. Harmless.
- **F7 (M0-5 impl note).** `wardrobeVersion`/`generationIndex` are ints; the length-prefix
  encoding `f"{len(s)}:{s}"` needs them `str()`-ified consistently before framing.

## R6 code edit landed
Brian gave the GO. `RANDOM_FRACTION = 0.7` deleted from `config.py` (replaced by a pointer
comment to the sampler's `random_count` helper); its assert dropped from `test_config.py`.
`python3 -m pytest` → **4 passed**. **M0-1 fully closed.**

## Next session: start here
Proceed to **M0-2 (data model)**. The `random_count(cap)` helper lands later with M1-3 in
`sampler.py` (not config). Carry F4 into M1-4, F5/F7 as implementation notes.
