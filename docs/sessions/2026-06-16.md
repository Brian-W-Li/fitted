# 2026-06-16 Session

## Goal
Documentation pruning: shrink doc surface and make doc-code consistency easier to maintain,
applying one explicit past-vs-future rule. Not a design review — no new resolutions, no Fable.

## What landed
- **CLAUDE.md** — Conventions now carries the *past goes to commits; future stays in docs* rule
  (with the trap-guard carve-out + R6 example). Canonical sources lists `docs/scope-decisions.md`
  and scopes `spec-resolutions.md` to PDF-ambiguity resolutions.
- **`docs/scope-decisions.md`** (new) — holds **R7** (host-not-frame) and **R8** (`sessionId = userId`),
  the two scope/boundary calls that were never PDF-ambiguity resolutions. R-numbering is shared and
  stable with the ledger.
- **`docs/plans/spec-resolutions.md`** — 51.9k → ~46.9k bytes. R7/R8 replaced by one-line stubs
  pointing to scope-decisions.md; review-history annotations stripped from R1–R13; Provenance
  paragraph → Reading guide; §4 provenance tags stripped. §4 critical scan: **no items were
  silently resolved** — all are genuinely-open milestone gates, so nothing was removed there.
- **`docs/plans/m0-m1-substrate.md`** — conservative pass: session-specific review attributions
  stripped (trap-guards preserved verbatim); one R8 path ref repointed to scope-decisions.md.
- **`docs/sessions/README.md`** — template trimmed to Goal / What landed / Next session (+ optional
  Follow-ups). Dropped Decisions/why and Blockers/open-questions.
- Commit **held for Brian** pending the per-R review-gate diff summary.

## Next session: start here
Resume the M1 build — **M1-3 (the 70/30 + cold-start `SignalScorer` seam)** per
`m0-m1-substrate.md` §4, built against **R13**. Reading list unchanged: `CLAUDE.md` +
`m0-m1-substrate.md` §4 + `spec-resolutions.md` R1/R4/R5/R10/R11/R12/R13. This was a docs-only
session; M0's 73 pytest remain the green baseline.

## Follow-ups
- **R2/R3 relocation — NOT done; needs Brian's call.** The session prompt suggested moving R2/R3
  out to `m0-m1-substrate.md` or a future M3 plan. I only stripped their annotations in place and
  left the bodies in the ledger, because relocating them conflicts with the single-home rule
  (R2/R3 are PDF-ambiguity resolutions — the ledger *is* their home; putting full bodies in the
  M0/M1 plan is wrong-milestone and restates a resolution in a plan doc) and "no new plan files"
  ruled out the M3-plan destination. Revisit if/when an M3 plan exists. *(spec-resolutions.md §2.)*
- **`legacy-prospecting.md` prune — deferred** (out of scope this session, per prompt). 17k bytes;
  same past-vs-future rule applies. Carries review-history annotations to strip. *(docs/plans/legacy-prospecting.md.)*
- **m0-m1 §1/§8 overlap.** §8 "Resolved confirmations" substantially duplicates §1 "Resolved
  decisions." Left intact (guard: be slow to cut the active plan doc); candidate to collapse later.
  *(m0-m1-substrate.md §1 + §8.)*
- **PDF→markdown conversion** of the v1.2 spec — superseded by `docs/Fitted_Spec_v2.md`; no conversion needed now.
