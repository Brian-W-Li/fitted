# 2026-06-15 Session — codex post-M0 review reconciled + M1 contract tightened (docs only)

## Goal
Check codex's post-M0 report (`CODEX_HANDOFF.md`), act on what's actionable now, and run a
full doc + code consistency/direction audit to decide the next step.

## What landed
- **Verified codex's report is credible** — spot-checked its "Verified in code" claims against
  source (auth/sync trusts body `firebaseUid` with no token; `images/[imageId]` does a bare
  `findById` with no ownership) — 2-for-2, not hallucinated.
- **Folded codex #1 (M1 sampler API) into the plan.** `m0-m1-substrate.md` M1-3 now returns a
  per-type `TypeSampleResult` (items + mode + reason + slot counts) instead of a bare list, and
  M1-5's `SamplerResult` is keyed by `ItemType` — forced by R11's own "logs never lie about
  *why* random ran" (a scalar return can't say type A sampled on signal while type B faulted).
  Five sub-questions resolved (see Decisions).
- **Doc fixes:** corrected the R5 occasion conflict in `legacy-prospecting.md` (occasion =
  normalized verbatim text, NOT a bucket — that rule is weather-only); added a legacy/v1.2
  split banner to `ml-system/README.md`; marked M0 done / M1 next in the milestone map;
  updated R10/R11 ledger headers to reflect real review status.
- **Memory** `fitted_v12_spec` updated: M0 complete, M1 build-ready, review status.

## Decisions / why
- **Independent-review gate cleared without Fable** (Fable unavailable this session). R11's pass
  was served by codex finding #1; R10 + the five M1 sub-questions got a first-principles design
  pass — all hold. Five resolutions: `is_available()` once-per-request (model presence is
  request-global); misbehaving `is_available()` → treat unavailable; `score()` must be finite
  **and** non-bool (R12 `warmth=True` precedent) else `signalScorerFault`; prompt pool order =
  `ItemType` enum order + id-sort within type; `RequestContext` built by the M5 adapter and
  passed in, not built by the sampler (keeps R5 normalization out of M1).
- **Next step = build M1, not `/spec`/`planner`.** The M1 plan (§4) already exists and is now
  patched; a fresh interview would re-derive it.

## Blockers / open questions
- None for M1. Codex's other findings are correctly parked: retained-host security (#2/#3/#4)
  → owned by M5 / retained-host gate (already in ledger §4); #4's data-integrity requirement
  feeds the **M4 `/spec`**; cache/regen lifecycle (#6/#7) → M5; eligibility/A-B (#10/#11) → M6.
- Fable remains unavailable — substitute with a fresh-context review agent if an independent
  second opinion on the M1 *implementation* is wanted once written.

## Next session: start here
**M1-1 (partition) + M1-2 (caps)** per `m0-m1-substrate.md` §4 — the two cheap deterministic
pieces — then build up to **M1-3 (the 70/30 + cold-start `SignalScorer` seam)**, the
load-bearing M6 plug point. Contract is settled in M1-3/M1-5 (per-type `TypeSampleResult`).
Read: `CLAUDE.md` + `m0-m1-substrate.md` §4 + `spec-resolutions.md` R1/R4/R5/R10/R11/R12.
Write tests alongside (pytest); M0's 73 tests are the green baseline.
