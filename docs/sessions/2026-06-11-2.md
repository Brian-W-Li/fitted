# 2026-06-11 (session 2) — prospecting, scope, doc governance (no code)

## Goal
Planning-only session: full prospective walkthrough of the project, Step B legacy-code
prospecting, scope decisions, and doc-governance hardening. No code written; M0 state
unchanged (M0-1/M0-2 committed, 17 tests green).

## What landed (all in canonical docs — this is pointers, not truth)
- **`docs/plans/legacy-prospecting.md` (new)** — Step B deliverable: legacy vertical read at
  source; failures F1–F14, at-risk features needing keep/drop calls, reusable assets,
  milestone routing (§5). Includes the W-track evidence (CV emits no occasions/seasons) and
  the B-track design (§3.9).
- **`spec-resolutions.md`** — new resolutions **R7** (host-not-frame; Gemini PreferenceSummary
  retires with the M5 fallback arm) and **R8** (sessionId = userId always; anonymous dropped).
  **R1** now specifies Steps 4–6 re-run per-request over the cached candidate stage. **R5**
  now splits: weather = canonical bucket, occasion = normalized verbatim text. §4 gained the
  W-track entry and deferred notes: repetition-window state (shownFullSignatures must be
  persisted — §15 schema lacks it), key-computation locus (keys computed once in Python,
  echoed response → interaction POST; no TS reimpl), M5 data path (Next POSTs wardrobe to
  Fly).
- **`CLAUDE.md`** — ingestion surface carved into scope (W-track); spec re-framed as
  **direction marker, not ceiling** (precedence: resolutions > PDF > legacy-as-reference);
  new **Doc lifecycle** section (single-home, living docs / edit-in-place, conflicts = bugs,
  default 3-doc reading list, retirement, ~1500-line compaction backstop, hole-finding moves
  into each milestone's `/spec`).
- **Ledger scrubbed to the truth standard** — supersession/amendment narrative removed
  (trap-guards kept), §5↔R5 conflict fixed, R6's stale "deletion pending" corrected against
  `config.py` (already deleted).

## Decisions (Brian)
- Greenfield rebuild rejected → R7 host-not-frame.
- Wardrobe ingestion revamp in-scope → W-track (CV/VLM extractor, async queue on the Fly box,
  unified review form), sequenced adjacent to M4/M5.
- Style boards → B-track: board replaces GPT's generic style priors as the style authority;
  staged B1 (description) → B2 (visual grid) → B3 (collage UX); B1 lands **after** the M5
  A/B baseline (contamination guard).
- Spec is inspiration, not constraint; ideas override via recorded resolutions.
- Docs are living: edit stale/wrong content in place; git is the archive.
- Global hole-finding passes concluded (ROI); continues as step one of each milestone `/spec`.

## Blockers / open questions
None. Committed (not pushed) at Brian's direction.

## Next session: start here
**M0-3 (keys)** per `docs/plans/m0-m1-substrate.md` §3 — then M0-4 (normalizer, the dense
one, do fresh), M0-5 (seed). Reading list: CLAUDE.md + the plan + `spec-resolutions.md`.
Carries unchanged from the previous session doc (F7 → M0-5 `str()`-ify int seed fields, etc.).
Note for M0-3: per the key-locus decision (resolutions §4), these Python key functions are the
only implementation of the key formats that will ever exist.
