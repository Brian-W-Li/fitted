# 2026-06-25 Session

## Goal
Close out the **Spearhead** milestone (orphan-item rescue cold-start vertical): a fresh-session
milestone-close audit of the uncommitted implementation, an adversarial second pass to falsify it, a
final cross-surface alignment sweep, then the doc/status sync + the milestone commit.

## What landed
- **Spearhead accepted and committed (C1–C6).** Three new modules over the closed M0–M3 substrate —
  `generation.py` (the `Generator` seam; lone lazy-`openai` IO boundary + `ReplayGenerator`),
  `rescue.py` (forced-item resolve/pin, H22 sufficiency, cold-start pool scoping, prompt build,
  generate→parse[one §12 repair]→validate→drop[forced+StyleMove]→rank), `response.py` (post-rank
  cold-start `compatibility`/`visibility` → `optionPath`/`risk` + the 2-D `(path,risk)` spread,
  `OutfitVariant`). Plus the C6 eval surface (`evaluation.py` external stage-attribution + telemetry,
  `cli.py` H40 manual run) and the golden stress corpus (12 cases under `tests/fixtures/corpus/`).
- **Hermetic suite: 666 pytest green** with `openai` absent. M0–M3 source untouched; only `config.py`
  (additive Appendix-B rescue constants) + `test_config.py` (guards) changed among tracked code.
- **Audit verdict: ACCEPTED.** No code fixes were needed; `RescueResult` stays product-shaped (telemetry
  is eval-only via `RecordingGenerator`/`GenerationCost`, never widens it); one repair fires on parse-fail
  only; GPT can't inject path/risk (validator forbids the fields *and* `response.py` recomputes them);
  H7/H21/H28/H42 and all M4/M5/M6 work confirmed deferred, none half-started.
- **Two real fixes found by the adversarial/alignment passes:**
  - `.gitignore`: the new `.env.*` rule was mis-ignoring the tracked `fitted/.env.sample` template;
    added `!.env.sample` (real secrets still ignored).
  - Spec §9 said path/risk labelling "land at the Spearhead milestone" (pending) while §20 says "✅ done";
    fixed the tense + added the `§20` pointer (conflicts-are-bugs).
- **Doc/status sync (§F applied).** `Fitted_Spec_v2.md` §20 Spearhead → ✅ done (3 modules + `Generator`
  seam + eval surface); §23 H20/H22 reconciled to *implemented*, H40 records the C6 live-eval verdict;
  plan `spearhead.md` carries `> COMPLETED 2026-06-25` + §E results + §F-applied; `ml-system/README.md`,
  `docs/README.md`, `CLAUDE.md` flipped Spearhead → done / next = M4.

## C6 / H40 live-eval (recorded, not reproduced this session)
Recorded from the prior live run (gpt-4o, `--runs 5`, 55 generations; reproduce via
`python -m fitted_core.cli --corpus-dir tests/fixtures/corpus --runs 5`, needs `OPENAI_API_KEY`):
100% JSON-parse, 100% forced-item inclusion, 100% StyleMove presence, 0 hallucinated ids, 0 schema
failures; latency p50 ≈ 2.60 s / p95 ≈ 5.11 s, ≈ 1,052 tokens/rescue, ≈ $0.0046/rescue (estimated).
Full detail in `docs/plans/spearhead.md` §E. This session ran only the hermetic suite — **no OpenAI calls.**

## Next session: start here
- **M4 — data-model migration** (`docs/Fitted_Spec_v2.md` §20, `[NEXT]`): `clothingType`→5 + backfill,
  action-enum extension, `ItemAffinity`/`wardrobeVersion`/`sessionId`, GenerationSnapshot, baseKey/fullSig
  on interactions, feedback-authenticity gate, the like→de-orphan loop. Holes: H10/H11/H19/H29/H37.
  `/spec m4` before building.

## Follow-ups (non-blocking)
- **Live-eval provenance:** the §E figures are a single recorded run with no committed artifact and are
  not exactly re-verifiable (GPT stochastic, H4). Treated as descriptive evidence per spec §21, not an SLA.
- **`OpenAIGenerator.last_usage`** is shared mutable state — benign now (eval-only, single-threaded,
  product never reads it); only a concern if M5 ever shares one generator across concurrent telemetry runs.
- **Plan §J** enumerates "486 unchanged + new test_generation/test_rescue/test_response" — true but
  non-exhaustive (omits test_evaluation/test_cli/test_config); left as a completed-plan reference.
