# 2026-06-18 Session (2)

## Goal
Recovery/authenticity audit of the post-crash v2 doc consolidation, then implement M1-3 (the SignalScorer seam).

## What landed
- **Audit clean.** The post-crash recovery is authentic: spec/plan/`fitted_core` source/CLAUDE.md/READMEs encode every locked decision (GPT/backend boundary, two-field `StyleEdge`, no Gemini/inferWhy, retired ledgers, v2 canonical). Only drift was stale comment pointers in `ml-system/tests/` (v1.2 §-numbers + retired-ledger citations) + one `§19`-as-edge-cases straggler — realigned to v2 numbering (commit `ad65ad9c`, comment-only, 0 assertion changes).
- **M1-3 done** in `ml-system/fitted_core/sampler.py`: `RequestContext` (M1 subset of §6.3), `SignalScorer` Protocol, `ColdStartSignalScorer` (never available → M6 plug point), `SelectionKind` enum (signal/random/includeAll), `TypeSampleResult`, `random_count(cap)` helper (integer half-up R6), and `sample_type(...)` — signal-first top-N + disjoint seeded random remainder, three behavior-identical/log-distinct fallback reasons (`coldStartSampling`/`signalUnavailable`/`signalScorerFault`), score-fault on raise/NaN/±inf/bool.
- `apply_cap`'s M1-2 callback seam was left intact (not reworked); M1-5's per-type loop will call `sample_type` directly and emit `include_all` results, retiring it (R13).
- **M1-4 done** (`sampler.candidate_requested(pool) -> int`): post-cap scaling — `total_base = n_tops*n_bottoms + n_dresses`; `<=5 → *3`, else `min(MAX_CANDIDATES=40, *3)`; outer/shoes never count; `total_base == 0 → 0` (the signal M1-5 short-circuits to `notEnoughItems` on, before any GPT call).
- **M1-5 done** (`sampler.build_candidate_pool(wardrobe, context, scorer) -> SamplerResult`): rejects duplicate ids (R12) → resolves `is_available()` **once** (raises/non-`True` → unavailable) → one shared `session_seed` RNG (R4) → partition → per-type `_sample_one_type` (include_all ≤cap / `sample_type` >cap) → `candidate_requested`; `not_enough_items` flag when 0; per-type `TypeSampleResult`s keyed by `ItemType`. **M1 complete.**
- The M1-2 `apply_cap`/`SampleFn` callback seam was **retired** (R13); `_sample_one_type` replaces it.
- **121 pytest green** (M1-5 added 9, retired 9 `apply_cap` tests).

## Next session: start here
1. **M2** (`docs/plans/m0-m1-substrate.md` §6 / v2 §8/§12/§13): SlotMap validation as pipeline Step 3 (wire `normalize_to_slotmap`/`is_valid_slotmap` + FullSignature dedup into a stage) **and** strict GPT-JSON schema validation of candidate output. Pure functions exist (M0-4); M2 is the pipeline-stage wiring. Do not start without re-reading the §12 prompt/output contract.

## Follow-ups
- The `scorer_available` boolean is a `sample_type` parameter today; the "evaluate `is_available()` once per request, misbehaving → unavailable" logic is M1-5's entry-point job (plan §4 M1-3 contract resolutions).
