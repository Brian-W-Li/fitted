# 2026-06-17 Session

## Goal
Recover safely from a Claude Code crash during the v2 documentation consolidation, preserve any remaining Codex brainstorm value, and make the active docs internally consistent before returning to implementation.

## Recovery History
- Brian arrived from a Claude Code crash mid-session. The prior chain was: Codex brainstormed `CODEX_HANDOFF` + `CODEX_USER_STORIES`; Claude integrated those, the old v1.2 spec, and other docs into `docs/Fitted_Spec_v2.md`; Claude then crashed during a final consistency pass.
- The first recovery task was preservation: compare the two distilled Codex brainstorm files against `docs/Fitted_Spec_v2.md` and extract anything not represented. That produced `docs/Fitted_Spec_v2_recovered_appendix.md`, intentionally separate from the implementation spec so ambition, anecdotes, dream notes, and user-story raw material are preserved without polluting build guidance.
- Claude had found a serious architecture inconsistency before crashing: some v2 sections had GPT assigning ranking/path/risk labels. This violated the hard rule that GPT never ranks. The recovery pass fixed that boundary: GPT may output candidate item roles, style text, matched/missing traits, and closed-set diagnostic reason candidates; Python owns scoring, ranking, path/risk, graph roles, freshness/exposure, and fallback decisions.
- Two delegated decisions were locked into v2: `StyleEdge` has exactly two fields (`compatibility`, `behavioralStrength`), and the Gemini / `inferWhy` / `inferredWhy` prose path is completely excised in favor of structured feedback reasons.
- Brian confirmed the Codex brainstorm concepts were adequately folded into v2, so the Codex brainstorm docs became retireable as active sources. `CODEX_HANDOFF` was restored as an ephemeral reusable handoff file after Brian pointed out it should not be erased as a tool.

## What Landed
- `docs/Fitted_Spec_v2.md` is now the canonical editable spec. It supersedes the old v1.2 PDF, `docs/plans/spec-resolutions.md`, and `docs/scope-decisions.md`.
- The old `docs/Fitted_Refactor_v1.2_Spec.pdf` is deleted from the worktree. The old ledgers are retired stubs pointing to v2 and its Appendix A concordance.
- Active guidance now points at v2: `CLAUDE.md`, `ml-system/README.md`, the M0/M1 plan, regen-controls, source comments in `ml-system/fitted_core`, and session/index pointers were updated.
- Historical docs (`DESIGN.md`, `MANUAL.md`, `RECOMMENDATION_MODEL.md`, `legacy-prospecting.md`) now carry superseded/historical banners instead of acting as active architecture.
- `CLAUDE.md` now describes the current arc: build `fitted_core` M1-M3, migrate/wire at M4/M5, prove orphan rescue, then train the graph scorer at M6. It also names `docs/Fitted_Spec_v2.md` as the single canonical spec and records the recovered appendix as historical context only.
- The active docs were read through, then verified with targeted scans. Clean verification: no active v1.2 PDF refs, no active Gemini/`inferredWhy` path, no split-edge fields, and no accidental GPT ranking authority. Remaining GPT/rank hits are guardrail language or backend re-rank wording.

## Important Fixes Made During The Audit
- Backend can rank K=10 while the UI may surface 2-3 primary options; those are no longer in conflict.
- Reliable / Bridge / Stretch are trust/progression lanes, not user-picked social-risk levels.
- `optionPath` and `risk` are Python-derived labels, never GPT output.
- `dislikePenalty` is documented as a positive magnitude that the formula subtracts.
- Feedback action semantics now acknowledge deployed `saved` / `worn` / `rated` and add `planned` / `packed` / `corrected` as v2 extensions.
- The M4 build-ladder row now includes the action enum extension.
- The M0/M1 plan now treats RequestContext as single-homed in v2 §6.3 and consumes only the M1 subset.
- Stale test-status and old section-reference notes were corrected in `CLAUDE.md` and `m0-m1-substrate.md`.

## Next Session: Start Here
1. Review `git status --short` and make one atomic docs commit for the v2 consolidation before further edits.
2. After the commit, resume implementation at M1-3 in `docs/plans/m0-m1-substrate.md`: the 70/30 sampler, cold-start fallback reasons, and `SignalScorer` seam.
3. Optional quick check before coding: run `pytest` in `ml-system` to confirm the M0/M1 substrate baseline is still green.

## Follow-ups
- The remaining real holes are intentionally centralized in `docs/Fitted_Spec_v2.md` §23. Highest-value future specs: W-track activation / `wardrobeVersion`, M5 `generationIndex`, M5 fallback semantics, daily reseed timezone, M4 idempotency, and rescue path/risk metrics.
- No tests were run in this recovery session because the changes were documentation and comments only.
- `docs/Fitted_Spec_v2.md` and `docs/Fitted_Spec_v2_recovered_appendix.md` are still untracked until the consolidation commit is made.
