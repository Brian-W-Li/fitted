# Session note — M5 C1+C2 build, review, and close-out (2026-07-06)

## What happened

- **C1 (daily orchestrator + generator params) and C2 (reducers + AffinitySignalScorer) are
  BUILT, reviewed, fixed, and verified.** Codex implemented; Claude ran a two-round review
  (an inline pass mid-flight, then a four-lane parallel audit: C1 correctness, C2 wire
  fidelity, test-quality/mutation, spec coherence) producing
  `2026-07-06-c1-c2-review-punchlist.md`; Codex executed the full punch list; Claude
  re-verified every item against source, including empirical mutant re-runs (scan-limit,
  `nSurfaced>0` filter, pinned digest constant, traced-daily StyleMove no-op — all killed).
- Landed shape highlights (all per the punch list / reconciled spec):
  - `RenderRequest` (alias of `RescueRequest`) with `intent`/`interaction_count`;
    `render`/`render_with_trace` dispatch (daily + rescue implemented; upgrade/translate
    raise `NotImplementedError`); daily prompt builders in `rescue.py`; the `_drop_invalid`
    split (StyleMove drop intent-generic pre-rank; forced-item drop rescue-only). Daily drops
    use intent-neutral `stylemove_invalid`/`render` provenance; rescue's `rescue_*` codes stay
    byte-stable.
  - `reducers.py`: §H reducers over `items` (NOT `itemIds`), public digested
    `COUNTED_ACTIONS`/`REJECTED_ACTION`, `REDUCER_CONFIG_VERSION` independent of
    `RANKER_CONFIG_VERSION`, `AffinitySignalScorer`. `_parse_created_at` accepts
    datetime/ISO-Z only (numeric epochs rejected — spec-pinned).
  - **`BehavioralSignals` → `RankerContext` plumbing landed through all four render paths**
    (was spec blocker C-B2: no checkpoint owned it; the behavioral layer would have shipped
    permanently cold). Defaults preserve golden byte-identity.
  - `gpt-5.4-mini`/`0.5`/`max_completion_tokens` defaults (H55); pricing entry
    (0.75, 4.50)/1M — externally verified (snapshot 2026-03-17). `PROMPT_VERSION`
    → `m5-c1.v1`, one shared version across intents (policy comment updated),
    `__version__` → 0.5.0. `MAX_JSON_NESTING_DEPTH=512` guard on the M2 parser with
    512/513 boundary tests.
- **Spec (`m5-cutover.md`) tightened in the same session** — review found 2 blockers +
  6 importants, all fixed: `sessionId` added to the wire contract/§F/§C.1 lineage (was absent
  everywhere); §C.1 derived-from-parent list gained `intent`+`forcedItemId` (rescue re-rolls
  would have silently become daily); pre-spend request validation vs degenerate-snapshot
  semantics pinned (§D); behavioralRows serialization pinned (verbatim camelCase, ObjectId→hex,
  Date→ISO, no numeric epochs); `M5_MAX_COMPLETION_TOKENS=900` pinned as C3 service config/env;
  duplicate-`requestId` retry response pinned to stored `candidates[]`+`itemSnapshots[]` via the
  same hydration helper; plus nits ($type wording, §J re-dispose sync, cooldown distinct-N).

## State

- Suite: **818 pytest passed** (floor 791→818; jest floor unchanged ≥387, untouched).
- Committed on main as two commits: the `ml-system/` C1+C2 close-out, then the
  `docs/` spec-tightening + session artifacts.
- §20 sequence position: consolidation → H26 ✅ → **M5 C1 ✅ C2 ✅ → C3 next**.

## What's next

- **OPENING GOAL (next session, before any C3 code): the C1/C2 convergence round.** The review
  loop stopped one round short of the CLAUDE.md fixed point: the punch-list fixes were verified
  as *executed*, but no fresh adversarial round ever ran on the FINAL post-fix code — the
  `behavioral_signals`→`RankerContext` plumbing, the rewritten reducer loops, and ~500 new test
  lines are unaudited surface. Run one fresh multi-lane round (correctness + test-mutation at
  minimum, spec-fidelity if cheap) over commits `b2877b85`+`2ef165c6`; fix and loop until a
  round returns zero load-bearing. Only then is C1/C2 *closed* — and the closing statement
  names residuals, not "all clean". Likeliest places a finding hides, honestly predicted:
  the behavioral_signals param interplay with the rescue wrappers' golden byte-identity claim;
  the new reducer edge-case tests asserting the wrong pinned semantics; C3-facing assumptions
  in the reconciled §A/§D text vs the landed validation split.
- **C3 — stateless HTTP service** (`ml-system/service/`): app.py + Dockerfile + fly.toml +
  service tests; §G.1 payload echo-through fields + serde (`parent_snapshot_id`, `constraints`
  et al.); `candidate_cache_key()` in seed.py + golden vectors; `variant_to_wire()`;
  `build_degenerate_payload()`; two-key auth + error envelope + §A service-side bounds
  (incl. the new pre-spend Lens validation: blank occasion / weather bucket / forcedItemId /
  intent set). Depends on C1+C2 (both done). Read the reconciled §A/§C.1/§D before building —
  they changed today.
- At C3 key provisioning: set the OpenAI project monthly budget cap (dashboard) — pinned in
  the C3 ladder entry.
- Then C4 (regenerate vertical + H28 seam) → C5 (Next integration, HEAVY AUDIT before+after).

## Blockers

- **None for C3.** All review findings are closed; the spec is reconciled with the landed code.

## Watch items (not blockers)

- v2 §20's M5-row entry-prereq wording ("H13 cross-runtime CI green") still conflicts with the
  plan (CI is a C8 deliverable) — the fix is folded into the scheduled C4/C5 §20-row rewrite
  (`m5-cutover.md` ~line 1212). Don't lose it.
- The daily prompt × `gpt-5.4-mini` mechanical read is REQUIRED pre-flip at C8 (two
  simultaneous deltas vs H40's rescue×gpt-4o numbers) — spec §B/§Verification pin it.
- `REPETITION_WINDOW_SNAPSHOTS` reducer bound is defensive only; the C3/C5 service/fetch
  queries own the `nSurfaced>0` filter + fetch bounds — their acceptance tests pin that.
- Legacy `itemIds` references remain in Next/C6-spec areas by design (the C6 route rewrite
  owns them); rescue-only `rescue_stylemove_invalid` goldens stay byte-stable by design.
