# 2026-06-21 Session

## Goal
Close out M3 with the C6 checkpoint (mutation hardening + status closeout) and land C5.

## What landed
- **M3 is complete.** The ranker (`ml-system/fitted_core/ranker.py`) ships Steps 4–6: per-request
  hard filters, additive scoring + signed `ScoreBreakdown`, diversity (variant cap → overuse →
  repetition), the fallback ladder, and the deterministic seeded tie-break. `rank()` returns a full
  `RankerResult` for non-empty input (the C1–C4 `NotImplementedError` stub is gone).
- **C5** landed earlier today (commit `57b80d09`): fallback ladder, tie-break, truncate-to-k, defensive
  `RankedOutfit` assembly.
- **C6** (this session): added three tests to `tests/test_ranker.py` — the one §12 mutant the C1–C5
  suite left uncovered (M3 must never re-dedup FullSignatures) plus two end-to-end `rank()` cases
  (overuse applied at the `none` stage; a non-empty pool hard-filtered to zero via the main path).
  Suite is **486 pytest green**; fitted jest unaffected (206).
- **Status flipped** to "M0–M3 complete" everywhere: `fitted_core/__init__.py`, `ml-system/README.md`,
  `docs/Fitted_Spec_v2.md` §20, `docs/README.md`, `CLAUDE.md`, and `docs/plans/m3-ranker.md` (now carries
  a `> COMPLETED 2026-06-21` banner and is off the default reading list).

## Next session: start here
- **Decide the post-M3 fork** (`docs/Fitted_Spec_v2.md` §20, both `[NOW]`): **Spearhead** (orphan-item
  rescue end-to-end — "proves the whole vision") vs **M4** (data-model migration + feedback-authenticity
  gate). `/spec <slug>` before building either.

## Follow-ups
- **`overuse_relaxed` is an unreachable terminal fallback stage** — score-only relaxation can't flip the
  count-based gate (documented in `ranker.py` `_select_fallback_pool` comment). If a doc-level note is
  wanted, plan §15 "known seams" is the home. Owner decision; not blocking.
