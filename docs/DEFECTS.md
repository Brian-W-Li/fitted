# Defect Register

Split out of `docs/Fitted_Spec_v2.md` §23 at S4a (`docs/plans/maintainability.md` D6a). This file is
the defect work queue plus its fixed archaeology; §23 keeps the design holes. No silent defects: a
newly found defect is registered here in the same edit that finds it (the same rule §23 states for
design holes).

**Classifier (a count is meaningless without one — maintainability.md §2):** a row lives here when its
subject is incorrect, missing, or harmful behavior of shipped code, tests, or ops measured against
their own intended contract — something whose closure is a **fix**. A row whose subject is an
undecided design question, a capability gap, or a spec/ambition conflict stays in §23 — its closure
is a **ruling or a build**. Pre-M5-era rows whose resolution text became spec contract (e.g. H14,
H55) stay in §23 with the design holes.

**Status vocabulary (D6c, closed set, enforced by hygiene check 8):** `**OPEN**` |
`**FIXED: <sha>**`. One row, one status; a row with a fixed half and an open half is split into two
rows. Severity and arming conditions live in the row body, never in the status — this vocabulary
deliberately has no `BLOCKED`: a defect is open until fixed, and "arms when X lands" is body prose.

**IDs and citations:** rows keep the single `H<n>` sequence shared with §23 — a new row takes the
next unused ID across BOTH files, and IDs never repeat across them. Rows here are cited as
`DEFECTS-H<n>`; §23 rows as `§23-H<n>`; `[[H<n>]]` links may point across the two files and resolve
by grep.

Row bodies are preserved in full from §23 — the byte win of the split comes from the move, not from
compression (compression of closed rows is S4b's, deliberately not done here). Where a row's old
free-text status carried more than the closed vocabulary (severity, landing notes), that text was
relocated verbatim into the resolution column as a leading italic parenthetical.

| ID | Defect | Status | Resolution / owner |
|---|---|---|---|
| H62 | **Dislike-enrich can resurrect a just-curated candidate.** The "tell us why?" enrich is a SECOND `rejected` POST landing ~1-3s after the one-tap dislike (`lib/useDislikeEnrich.ts`). Because `deleteInteraction` hard-deletes every row for a `{snapshotId,candidateId}` binding, an enrich still in flight when the friend REMOVES that card in History simply `.create()`s a fresh `rejected` — resurrecting a curated candidate for the whole enrich round-trip (the flip case is safe; only remove). | **OPEN** | *(DOCUMENTED — narrowing OPEN (backlog).)* Bounded + recoverable: the row reappears in History (re-curatable) and the dashboard reconcile closes the dashboard-return case, but an M6 export pulled inside the window captures a label the friend believed erased. The `useDislikeEnrich` docblock now states this honestly (was mis-scoped "practically never"). Fix (backlog): an `AbortController` on the enrich POST tied to dashboard component lifetime, aborting the in-flight enrich on navigate/curate. Do NOT add cross-load persistence of held reasons without re-solving this (it widens the window to a routine one). |
| H63 | **Account-DELETE claimed full erasure even when Firebase auth deletion failed.** `DELETE /api/account` swallowed an `adminAuth.deleteUser` throw and returned `{ok:true}`, so a transient Firebase outage left the user's Google email/displayName/photoURL in Firebase Auth (the only residual identity after Mongo is wiped) while telling the user they were fully deleted. | **FIXED: 30b03cc9** | *(RESOLVED — honest partial-failure LANDED (`app/api/account/route.ts`).)* The route now retries `deleteUser` once (300ms backoff) and, if it still fails, returns `502 {ok:false, dataDeleted:true, authDeleted:false, error:"auth_deletion_failed"}` — never claiming full erasure while the identity survives. All Mongo data is already gone by then; the user can re-sign-in (`auth/sync` re-creates a fresh empty user, old data does NOT return) and delete again to retry only the auth step. **Client copy LANDED:** the 502 branch now `alert()`s an honest message (`PARTIAL_DELETE_MESSAGE`, `account/page.tsx`) naming the surviving sign-in + the retry path, instead of silently signing out — mount-tested (`accountPartialDelete.test.tsx`, mutation-proven). **Residual (deferred, backlog):** the surviving Firebase Auth identity (email/name/photo) is a *retention* gap, not an *exposure* leak — it is in no app DB, returned by no endpoint, reachable only by that person's own Google login; and token revocation only fires as a side-effect of the (failed) `deleteUser`, so it is not revoked in this path. The only close for the "user never returns to re-delete" case is a durable server-side retry (a background job / cron the project lacks); at 3–5 friends the 502 needs a Firebase outage spanning both attempts at the delete instant — a near-never event — so building that infra is deferred until the study scales. |
| H64 | **GenerationSnapshot's "no blob bytes EVER" boundary is enforced on only 3 raw fields.** `rawText`/`rawEmitted`/`rawAttributes` are byte-capped, but sibling service-authored passthroughs — `itemSnapshots.generatorVisible` (Mixed), `embeddingRef`/`visualFeatureRef` (unbounded String), `diagnostics.ranker`/`diagnostics.rescue` (Mixed) — are uncapped/unvalidated, so a buggy/compromised service could persist a blob into the immutable corpus. | **OPEN** | *(latent (shape decided).)* Latent today: the pinned Python service populates none of these (`snapshot.py` emits only `item_id`+`engine_visible`), so the exploit needs a compromised/severely-buggy service; the 16MB BSON ceiling backstops only the DoS half. Decided fix (land with the next session touching `lib/mlSnapshotMerge.ts`): **reject-if-present** for the three reserved-and-unused fields (mirror how `lens`/`constraints` are M5-gated absent — the stronger invariant), plus a **byte-cap** on `diagnostics.ranker`/`rescue` (legitimately populated → cap not reject). |
| H65 | **No prod index-build/parity mechanism.** `lib/mongodb.ts` sets `autoIndex:false` in prod (correct — avoids cold-load re-sync stalls) but there is no `syncIndexes()` deploy step and no CI/health check that live indexes match the schema, so a FUTURE schema index change silently no-ops in prod. The load-bearing one is the partial-unique `{user,requestId}` idempotency index (H50) — if absent, a concurrent double-submit inserts two corpus rows for one render. | **OPEN** | *(no active break.)* A live check confirmed the `{user,requestId}` index currently EXISTS on prod Atlas, so nothing is broken now; erasure is unaffected (cascade deletes by `{user}` regardless). CI-artifact hygiene ("enforce process rules with tests, not discipline"). Trigger: **before the next deploy that changes any model's indexes** — a `scripts/sync_indexes.mjs` runner (`Model.syncIndexes()` per model) + a parity assertion diffing schema-declared vs live indexes. |
| H66 | **Geo weather discarded temperature.** With a location present, `resolveWeatherProd` fetched a rich summary (e.g. "Clear sky, 34°C") then bucketed it via `bucketFromSummary`, a condition-TEXT keyword matcher — the WMO summaries contain no hot/mild/cold keyword except "snow", so every non-snow temperature collapsed to "mild". The bucket is the only temp signal reaching the stylist prompt and the `WEATHER_WARMTH_BAND` option-path display bucket (`fitted_core/response.py:208-209` — **not** a ranker penalty; the ranker reads `context.weather` only as a tie-break RNG seed input, §23-H105), so a 34°C day was styled "mild" and the M6 corpus recorded `weather:"mild"` for hot/cold renders. | **FIXED: 5298997f** | *(RESOLVED — numeric bucketing LANDED (`lib/weather.ts` + `lib/mlRecommend.ts` `bucketFromTemp`).)* `getWeatherContext` now returns `tempC`/`feelsLikeC` (center-slot for the forecast path); the geo path buckets by the NUMBER, feels-like preferred: **cold ≤ 10°C, hot ≥ 24°C, mild between** (aligned to the warmth bands), with a **snow-in-summary → cold** override. `bucketFromSummary` stays the no-geo occasion-text fallback. Fulfills the m5-cutover §F "temp/condition → bucket" mandate (was half-implemented). Thresholds AND the `resolveWeatherProd` geo→`bucketFromTemp` wiring are pinned in `contextDetection.test.ts` (the wiring test is mutation-proven: reverting `lib/mlRecommend.ts:158` to `bucketFromSummary` reddens it). |
| H67 | **Atlas M0 aggregate capacity is unbounded by the per-user guards.** Every per-user bound exists (wardrobe 300, images 80MB raw-byte ledger, interactions 2000, renders 12/min/instance sustained (burst 5)) but nothing bounds the AGGREGATE against the shared 512MB M0: (a) images are stored **base64 (×4/3 on disk)** and the 80MB budget sums raw bytes, so one at-cap account really occupies ~107MB — **~4.8 at-cap accounts fill the cluster**, and open Google sign-up makes account count attacker-controlled; (b) **GenerationSnapshot has NO per-user count ceiling** (unlike its three siblings) — an authed loop riding the limiters accretes rows indefinitely (each ~15–50KB with raw attempt text). When M0 fills, writes fail **cluster-wide**, including for friends. (2026-07-20 dynamics audit.) | **OPEN** | *(abuse-shaped, not friend-shaped.)* Cooperative 3–5-friend scale is comfortably inside M0 (real closets ≈ 15–50 photos ≤1MB downscaled). Design calls when picked up: `MAX_SNAPSHOTS_PER_USER` mirroring the interactions countDocuments→degrade pattern (degrade semantics at ceiling, never 5xx), and either lower `MAX_USER_IMAGE_BYTES` to ~50MB (6 accounts × 50 × 4/3 ≈ 400MB) or an aggregate headroom check. Interacts with NEW-D (M0→M2 upgrade decision) — an M2 mostly dissolves (a). Do NOT solve with a signups cap alone; the snapshot ceiling is wanted regardless. |
| H68 | **The render service executes renders ON its single ASGI event loop — effective concurrency 1.** `async def app` calls `handle_render` synchronously and the generator is the blocking sync OpenAI SDK call (30s timeout), with no executor dispatch and 1 uvicorn worker on the 1-machine pin: concurrent renders queue serially and `/readyz` (same loop) starves behind them. ~5 simultaneous first renders → 40–50s tail → the Next 45s abort shows "stylist unavailable" on a friend's FIRST render, with the OpenAI spend already committed. (2026-07-20 dynamics audit; the serial behavior is PINNED by `service/tests/test_serialization.py`.) | **OPEN** | *(mitigated by ops rule.)* Mitigation (runbook §8 pre-recruit checklist): stagger onboarding, one active friend at a time — sufficient for 3–5 cooperative friends. The fix is a design call, not a patch: dispatch `handle_render` via `asyncio.to_thread`/executor (or uvicorn workers) **re-examining what it does to the global token bucket** (in-process state — workers would shard it) and the `/readyz` health contract. The pinned serialization test is EXPECTED to flip when the fix lands and must be rewritten to assert the opposite order — that flip is the fix's proof. |
| H69 | **No pixel-dimension bound on stored images — a decompression bomb passes the sniff.** Upload guards are strong (magic-byte sniff, content-type allowlist, byte budget, nosniff serve) but nothing reads IHDR/SOF dimensions: a KB-sized valid PNG declaring 50000×50000 px is accepted and stored. Today's decode is client-side own-account only (self-inflicted broken tile); the REAL trap is the **M6 image-embedding pipeline**, which will decode stored bytes server-side (~7.5GB alloc for the example). (2026-07-20 dynamics audit.) | **OPEN** | *(gates the M6 decode path.)* The M6 embedding session MUST bound decoded pixel dimensions before decoding corpus images (parse PNG IHDR / JPEG SOF and reject > ~6000px per side, or decode via a bounded-memory path) — register this in the embedding pipeline's plan when it is specced. Optionally close at upload (`lib/imageStorage.ts` dimension parse → 415) if the wardrobe surface is touched first; friend-scale urgency is low either way. |
| H72 | **Test-coverage residuals from the 2026-07-24 test audit (deliberately deferred, not gaps in shipped behavior).** A full test-forgery audit (blind use-case derivation → per-cluster adversarial audit → source verification) found NO forged tests and hardened the five load-bearing gaps (C1–C5 of that batch: frozen-artifact rewrite guard, `verifyUserProd` auth arm, `wardrobe/clear` invalid-token 401, PATCH `/account` validation, frozen decision-rule pin). These are the KNOWN-uncovered surfaces it left, each judged low-risk or compensated elsewhere: **(rescue-intent controls)** locks / dislikes / unbuildable-gate proven only via daily renders in `ml-system/tests` (partially compensated in `service/tests`). **(service)** the last-resort 500 wrapper (`ml-system/service/app.py:1031-33`), auth-before-throttle ordering (`app.py:1015-17`), and controls dedup/canonical-order (`app.py:374`) — untested. **(UI)** ~~the `cvUnavailable` footer branch~~ **— NO LONGER UNTESTED (verified 2026-07-25):** `lib/addItemUploadStepActions.tsx:51-89` is now exercised end-to-end, because CV-off is the production default and the real-page suite rides it — `tests/wardrobeAddAnotherPhotoLoss.test.tsx` serves `/api/cv/status` → `not_configured` and clicks the CV-off-only "Continue →" primary in five tests, and `tests/addItemModal.test.tsx` renders the upload step with `cvUnavailable` explicitly. What is still genuinely unpinned is only the DISABLED-state matrix in `tests/addItemUploadStepActions.test.ts`, which asserts `disabled === true` twice and never the one state that must be clickable (file present, not analyzing) — so `disabled={true}` would ship a permanently dead Analyze button, green. Low cost while CV is off; close it with the W-track CV service; history-curation unbound-row guards + DELETE-404-as-success (`app/(app)/history/page.tsx`); ~~client-side image validation (`prepareImageForUpload` downscale / >4MB throw)~~ — **COVERED 2026-07-25** (`addItemModal.test.tsx`: the downscale under a stubbed `createImageBitmap`/`toBlob`, and the pick-time upload-gate rejection). **(experiments)** `select_over_grid` tie-break asserted only via an inline `max()` mirror (`experiments/h26/tests/test_train_head.py:174-192`; low risk — selection sealed); `mlSnapshotValidation.test.ts:71-75` duplicate/vacuous case; `localServiceSmoke`'s `if (body.bindable)` soft-pass (`:132-141`). | **OPEN** | *(deferred, low-risk.)* Pick up opportunistically when the owning surface is next touched (the UI branches with the W-track ingestion work; the service arms with any service change; the experiments mirrors are cosmetic given sealed selection). Not friend-blocking. |
| H73 | **F10 resume can false-409 on a geo-render bucket flip (narrowed, not closed).** The dashboard's pending-render envelope (`dashboard/page.tsx` `buildRootBody`) re-sends `{lat,lon}` on a dropped-response resume, never the resolved weather, so the route re-resolves live. The **2026-07-24 fix removed `weatherRaw` from the §C.4 render identity** (`lib/mlSnapshotMerge.ts` `RenderIdentity`/`identityMatches`) — a volatile provenance string that does not shape generation and was the common false-409 cause. The **bucketed `weather` stays** in the identity (it genuinely shapes the render), so a resume that straddles a temperature-bucket boundary (e.g. 23.9→24.1°C, mild→hot) still 409s "requestId reused for a different render" and re-spends instead of replaying the already-paid render. (2026-07-24 recommend-path audit.) | **OPEN** | *(narrow residual, geo-only.)* Geo-granted friends only (minority per [[H71]]), resume path only, no data/corpus loss (the paid snapshot is a valid corpus row; the user re-spends). Complete fix: echo the resolved `{weather,weatherRaw}` into the browser response + envelope so a resume re-sends the FROZEN bucket (the F10 design the code diverged from) — a post-M5 nicety, not friend-blocking. |
| H74 | **Writer-side erasure guards are check-AFTER-write: a crash between the row insert and the `User.exists` check leaves a permanently unsweepable orphan.** All four writers insert first, then self-erase if the user vanished (`lib/interactions.ts:277→296`, `lib/mlRecommend.ts:569→584`, `app/api/wardrobe/route.ts:215→236`, `app/api/wardrobe/[id]/image/route.ts:171→196`). If DELETE /api/account finishes its phase-3 sweep and a concurrent already-authed write lands its row, then the serverless function is killed before the `exists` check runs, the row survives under a dead user's ObjectId forever — auth now 404s that uid, re-sign-in mints a new `_id`, and no future account-delete matches the orphan; there is no offline sweep for un-redacted rows whose `user` resolves to nothing. Millisecond window, needs a crash + interleave. (2026-07-24 data-loss audit.) | **OPEN** | *(tiny window, breaks "delete me" in the residue.)* Cheap close: add an offline orphan check (rows whose `user` resolves to no User) to the existing cron/corpus verifier (`scripts/track2-monitor.mjs` or the gated read-back), redacting/deleting any found. Design + a deployed cron artifact — do not bolt onto a live route. |
| H75 | **D2 keep-referenced-photos has a check-then-act race against an in-flight render: a snapshot can be written referencing an image deleted seconds earlier.** The reference checks read snapshot state BEFORE deleting (`lib/clearWardrobe.ts:32-35`, `app/api/wardrobe/[id]/route.ts:239`, `app/api/wardrobe/[id]/image/route.ts:144` reads and `:189` acts), but a render's snapshot write lands at the END of a 10–45s pipeline (`lib/mlRecommend.ts:569`). If a user taps Generate then (mid-render) deletes the item / clears the wardrobe, the reference check finds no snapshot referencing image X (the referencing snapshot doesn't exist yet) → deletes X → the render then persists `itemSnapshots…imageRef="mongo:X"` pointing at nothing, silently voiding the labeled outfit's image side at M6. Window = full render duration; "delete right after generating" is plausible friend behavior. (2026-07-24 data-loss audit.) | **OPEN** | *(wide window, silent M6 image-side loss.)* Fix is a design call: re-verify (and re-retain) referenced images AFTER the snapshot write, or make the M6 export treat a since-deleted referenced image as `unresolved` (it may already — verify the D2 export path resolves from the retained ref, else count it loudly). Adjacent to [[H69]] decode bounds. Not a "delete me" violation (over-deletion, not under-deletion). |
| H76 | **The Next→service render timeout budgets ONE OpenAI call; the service lawfully makes TWO.** `OPENAI_TIMEOUT_SECONDS=30` per call (`service/config.py:49`) and the engine makes an initial + one blind repair call (`fitted_core/rescue.py:1139,1152` and `:898,902`), so the service's own worst case is ~60s. But Next aborts at `SERVICE_TIMEOUT_MS` (default 45s, clamped ≤50s — `lib/mlServiceClient.ts`) and the margin test only asserts `≥ 1× openAiTimeout + 5s` (`crossRuntimeContract.test.ts`, single-call premise). A slow first attempt + repair crosses 45s → Next degrades `service_unavailable` while the service finishes into a closed socket; since Next is the sole snapshot writer, the completed (two-paid-call) render is persisted NOWHERE — unlike every other failure arm, which writes a degenerate corpus row. Parse-fail-then-repair correlates with messy real closets — exactly the failure corpus M6 wants. Deterministic: 45s (even the 50s clamp) < 2×30s. (2026-07-24 recommend-path audit.) | **OPEN** | *(deploy-config decision (do not hot-edit live).)* Resolution options: shrink `OPENAI_TIMEOUT_SECONDS` so 2×it + overhead < `SERVICE_TIMEOUT_MS` < `maxDuration` (e.g. 18s→41s<45s), or bound the service's TOTAL wall-time under Next's budget, or correct the margin test's premise to the 2-call worst case so CI reflects reality. All touch deployed Fly/Vercel config → set both sides in one change (runbook §8 warns of the drift), Fable-review the timeout math. |
| H77 | **Ingestion yield-loss + dead-end on the friend-critical add path (W-track UI).** Two friend-visible defects in `app/(app)/wardrobe/page.tsx`: **(a)** a photo-upload failure during "Save & add another" was invisible — the handler called the PAGE-level `setError` (banner rendered behind the modal's `fixed inset-0 z-40` overlay) but returned success, so the modal reset and the item saved PHOTO-LESS with nothing on screen; on a batch-add of a 15–30-item closet a rate-limit 429 / >4MB-after-compression throw / flaky network then repeated silently — direct yield loss on the M6-image-critical path. **(b)** the 5MB pick-time cap (`onPickImage`) ran on the ORIGINAL file before `prepareImageForUpload` (the 1280px downscaler built to solve exactly this) ever ran, and the app's own HEIC advice ("set Camera → Most Compatible" → multi-MB JPEGs) steered friends into that dead-end with a remedy-free "Max image size is 5MB." (2026-07-24 ingestion audit.) | **FIXED: df9d8f1f** | *((a) + (b) RESOLVED — LANDED 2026-07-25 (friend-first-use hardening pass); the residual siblings split to [[H107]].)* **(a)** `onSave` now returns a third outcome, `{ savedWithPhotoWarning }` (`page.tsx` `SaveResult`/`isSavedWithPhotoWarning`), emitted by BOTH the add- and edit-branch photo catches AFTER their cleanup tails run — returning early from the catch would skip them and strand the modal on the stale CV step. (The EDIT branch has no "add another", so its object always lands on the close path and the in-modal notice never paints there; its friend-visible signal is the page banner, which is what the edit test pins. The edit-branch `return` itself is contract-uniformity only and is **deliberately NOT pinned** — mutation-verified: deleting it leaves the suite green, because it has no observable effect in edit mode. Keep it so the branch cannot report a photo failure as a clean success if edit ever gains a reset path; do not mistake it for tested behavior.) `submitForm` treats it as success (the item EXISTS; keeping the old values would invite a duplicate-minting re-save) while appending to `photoWarnings`, an amber notice in the always-visible `shrink-0` footer. The list **accumulates and is never cleared by a reset or a later clean save** — it is a to-do list, not a toast: a 429 burst can lose five photos and the friend needs every item NAME to act. Shape Fable-reviewed (minimal union-widening chosen over a full discriminated union: the defect was a swallowing catch, which the richer type would not have prevented). Regression-pinned end-to-end over the REAL page handler in `wardrobeAddAnotherPhotoLoss.test.tsx` (mutation-proven: deleting the handler's `return` reddens it) + the modal-side contract in `addItemModal.test.tsx`. **(b)** the pick gate is now a `MAX_PICK_BYTES = 40MB` sanity ceiling only; the real limit is the existing post-compression 4MB throw, so the downscaler finally runs on the multi-MB JPEGs it was built for. Because the raised ceiling would otherwise put a ~53MB base64 data URL in React state on a phone, **the preview is generated from the downscaled copy** — and that same effect is now where the REAL limit is enforced, because it is the first place the post-downscale size exists: a result over **`MAX_UPLOAD_BYTES` (4MB, single-homed and shared with `uploadWardrobeItemImage`)** means `prepareImageForUpload` fell back to the original (no `createImageBitmap`, an OOM, or a re-encode that didn't shrink), so the pick is **rejected then and there** with the screenshot remedy. Failing at pick costs one message; letting it through cost a created item, a failed photo, and a retry that loops on the same unusable file. A surviving file is ≤4MB, so its preview is ≤~5.3MB and no separate preview ceiling is needed. The preview is also cleared on every pick change, and **whenever a NEW file is pending** `photoPreviewSrc` resolves to that file's preview or nothing — never the stored image — so the UI can't assert photo A while photo B is what saves. (The qualifier is load-bearing: with NO pending pick, edit mode still shows the stored photo, which is correct and is what `willHavePhoto` depends on. The code is `imageFile ? previewUrl : existingPhotoUrl` — a ternary on pendency, not an unconditional suppression.) `imageFile` identity is untouched, so the CV-crop `imageFile === addPendingFile` comparison still holds. Pinned in `addItemModal.test.tsx` **with a stubbed working `createImageBitmap`/`toBlob`** — jsdom has neither, so without the stubs the downscale silently falls back and the test would pass either way (this pin shipped vacuous once; do not remove the stubs). |
| H78 | **`imagePath` is PATCH-able as an arbitrary string — no shape gate, no ownership check.** `lib/wardrobeRequestValidation.ts` puts `imagePath` in `PATCH_STRING_FIELDS`, so it gets only the generic 2048-char + well-formed-UTF16 check; there is no `mongo:<24-hex>` shape gate and no check that the id names an image the caller owns. Any authed user (sign-up is open Google auth) can PATCH `imagePath: "mongo:<another friend's imageId>"`. The next render embeds that ref in their snapshot's `itemSnapshots`, and `scripts/exportTrack2Core.cjs` resolves `wardrobeimages` **by `_id` with no `user` filter** — so the foreign photo bytes land in the M6 bundle attributed to the wrong friend. NOT an exfiltration path (`/api/images/<id>` still 404s a non-owner, so the attacker never sees the photo) — the damage is corpus contamination + identity mixing inside the export. Found by the 2026-07-25 verification audit; **no client path sends `imagePath` in a PATCH** (`submitForm` builds its own payload without it), so the field looks removable. | **FIXED: 77570c2b** | *(WRITE SIDE RESOLVED 2026-07-25 — removed from `PATCH_STRING_FIELDS`, two mutation-proven tests in `wardrobeEditIngestion.test.ts`; the read side (the exporter's missing owner check) split to [[H108]].)* Cheapest correct fix: drop `imagePath` from `PATCH_STRING_FIELDS` entirely (verify no caller first — a contract narrowing, so Fable-review it). If it must stay, gate it on `OBJECT_ID_RE` after the `mongo:` prefix AND a `WardrobeImage.exists({_id, user: userId})` check. Independently, add a `user` filter to the exporter's image resolve and extend `exportTrack2.test.ts`'s userFilter-scoping test to `wardrobeimages` — today that test covers the three row collections and skips the one carrying actual photo bytes. |
| H79 | **`prepareImageForUpload` runs TWICE per pick, and its EXIF fallback makes big photos unuploadable on older browsers.** (a) The preview effect decodes+re-encodes to discover the post-downscale size, then `uploadWardrobeItemImage` does it AGAIN from the original — two full 12MP decode + canvas re-encodes per item, seconds each on a phone, ~30 for a 15-item batch. They can also disagree under memory pressure: a preview that downscaled fine while the upload's decode OOMs falls back to the multi-MB original and throws `TOO_LARGE_TO_UPLOAD_MSG`, so the item saves photo-less (honestly reported, but avoidably). (b) The decode asks for `imageOrientation: "from-image"`; browsers that shipped only the older `{"none","flipY"}` enum throw, and the fallback correctly returns the ORIGINAL (re-encoding would strip EXIF and bake a sideways photo, silently halving the M6 image signal — §23-H53). Correct, but the cost is that on those browsers any original over `MAX_UPLOAD_BYTES` (4MB) is refused with screenshot-only as the remedy. (2026-07-25 verification audit.)  **NARROWED 2026-07-25 by direct engine testing** — the shipped function's source, extracted verbatim and run in BOTH engines: **Chrome and WebKit (Safari's engine) both accept `"from-image"`, both have `createImageBitmap` + `canvas.toBlob`, and both bake EXIF Orientation=6 correctly** (proven differentially: identical pixels ± the tag ⇒ transposed output). So the yield cliff is confined to genuinely OLD engines, not to Safari as such. One real cross-engine finding to keep: WebKit's JPEG encoder is much less aggressive at q0.85 — the same 12MP source lands at **~1.0MB on WebKit vs ~455KB on Chrome**, so the margin under the 4MB gate is ~4x, not ~9x. Ample, but do NOT reason from Chrome's numbers when changing `MAX_EDGE_PX`, the quality factor, or the gate. **Residual: Playwright's WebKit is not iOS Safari** — same engine family, different build; it settles API support and pixel behaviour, NOT iOS memory pressure or tab-discard, which is what the blob-vs-data-URL rationale rests on. | **OPEN** | *(friend-facing slowness + a browser-dependent yield cliff.)* (a) Cache the prepared File against its source File (a ref keyed by identity) and let the upload reuse the preview's result — halves the decode work and removes the disagreement window. (b) Feature-detect once, and on a `from-image` failure fall back to an `<img>`-element decode: browsers apply EXIF orientation to `<img>` rendering, so `drawImage(imgEl)` yields upright pixels without needing the option. Both are W-track ingestion work, not audit fixes. |
| H80 | **A successful image store followed by a failing repoint/cleanup leaks an unreachable WardrobeImage — and can report success as failure.** `app/api/wardrobe/[id]/image/route.ts` is store → repoint → delete ([[H14]], correct). But if the `WardrobeItem.updateOne` repoint throws, the new row exists with nothing pointing at it: an orphan that counts against `MAX_USER_IMAGE_BYTES` forever with **no UI that can free it**, and each retry leaks another. If the old-image `deleteOne` throws instead, the upload actually SUCCEEDED and was repointed, yet the outer catch returns a generic 500 — the friend sees "its photo didn't upload" and may re-upload, leaking again. Needs a DB failure mid-route, so the window is small; same family as [[H74]] (check-after-write orphans with no offline sweep). (2026-07-25 verification audit.) | **OPEN** | *(small window, unbounded orphan accrual.)* Fold into H74's fix rather than bolting a transaction onto a live route: the orphan-detector that H74 needs (rows whose `user` resolves to nothing) should also flag `wardrobeimages` rows no `WardrobeItem.imagePath` and no `GenerationSnapshot` references — one offline sweep covers both. Separately, narrow the cleanup `deleteOne` into its own try/catch so a cleanup failure logs and returns 200 rather than misreporting a stored photo as lost. |
| H81 | **`clearWardrobe` is non-atomic and its partial state is invisible.** `lib/clearWardrobe.ts` deletes `WardrobeItem`s FIRST, then computes `referencedImageIds`, then deletes the unreferenced images. If either later step throws, the route returns a flat 500 "Failed to clear wardrobe" — so the friend reads "nothing happened" while their entire closet is already gone AND orphan image rows keep consuming their 80MB budget, with no surface that can free them (every future upload then 413s "Image storage limit reached" against an empty closet). `wardrobeClear.test.ts` asserts both deletes happen and the `$nin` filter, but nothing pins the ORDER and nothing fault-injects a mid-sequence failure. (2026-07-25 verification audit.) | **OPEN** | *(misreported destructive outcome.)* Either delete images first (they are the recoverable-by-nobody half, so losing the pointer is worse than losing the row) or report partial success honestly the way [[H63]]'s account-delete branch does: `{ ok: false, itemsDeleted: n, imagesDeleted: m }`. Add an order pin + a fault-injected mid-sequence test in the same change. |
| H82 | **Stated guarantees with no enforcing test (the residual coverage backlog after the 2026-07-25 audit).** That audit closed the highest-value ones with mutation-verified pins — the erasure door (`tests/dbErasureDoor.test.ts`: the real `deleteUserWithData` + the `initDatabase` init list, previously executed by NO test because 12 suites mock `@/lib/db`), image `Cache-Control: private`/`nosniff`, `GET /api/wardrobe` cross-user scoping, the session cookie's `Secure` flag under a production NODE_ENV, all four irreversible-deletion `confirm` gates, the store→repoint→delete order, and six one-sided numeric thresholds. **Still unpinned, roughly by cost:** the dashboard's like/dislike failure revert + error (`markFeedbackClear` — the primary M6 label path; a fire-and-forget regression loses labels silently and `persistResult` then writes a phantom chip); the F10 envelope's write-BEFORE-fetch and keep-on-dropped-response (a regression mints a second PAID render per friend action); the degraded-re-roll no-wipe WIRING (`renderResultGuards` tests the predicate only, not that `onResult` returning `false` is honored); `AuthGate` (zero tests — redirect, session-cookie-before-children, and the `syncUser` repair net); `DISLIKE_REASONS` ⊆ `FEEDBACK_REASON_CODES` (a typo 400s every structured "why" while the one-tap still lands, so the corpus degrades to reason-less rejections); `RENDER_RATE_MAX_PER_MIN` vs the service's global bucket; cascade COMPLETENESS (a sixth user-owned collection would be silently exempt from "delete me"; `scripts/track2-erasure-check.mjs` holds a second hand-copy of the list); `/api/auth/sync`'s `displayName`/`photoURL` bounds (the app's widest untrusted-body surface — it runs before a User row exists); `sessionStorage.clear()` on erasure; the history load/curation error paths; `/api/cv/infer`'s 429; the availability-toggle error; and `addItemUploadStepActions`'s enabled state (asserted `disabled === true` twice, never false — so `disabled={true}` ships a dead Analyze button). | **OPEN** | *(session-2 lane (app deep audit).)* Work it in cost order, not list order. Two structural notes: `lib/db.ts` is no longer mock-only, so the `dbErasureDoor.test.ts` pattern (real `connectMongo` against an in-memory mongod, no `@/lib/db` mock) is the template for anything in that module — but do NOT call `jest.resetModules()` there, it gives `mongoose` a second uncollected instance and jest never exits. And do not inherit the cross-file-leak reasoning that cleanup was first written with: jest builds one environment per test FILE, so `globalThis` is per-file and NOT shared across suites even under `--runInBand` (probe-verified 2026-07-25). And prefer cross-file INEQUALITY pins (the `UPLOAD_RATE_MAX >= CREATE_RATE_MAX` / `MAX_ITEMS_PER_USER <= MAX_WARDROBE_ITEMS` idiom) over restating a constant, which is a mirror. |
| H83 | **The CV analyze path has three staleness defects that surface the moment CV is switched on.** All DORMANT today (CV is `not_configured` in production), all in `app/(app)/wardrobe/page.tsx` + `lib/addItemUploadStepActions.tsx`: **(a)** the modal's `onClose` prop and the card's `onEdit` handler clear `addPendingFile` but never `cvAbortRef.current?.abort()` — only the "+ Add item" button handler does. (Cited by SYMBOL, not line: these rows' line numbers drifted twice within one day, including by the commit that wrote them. Grep the symbol.) A late `/api/cv/infer` response then re-arms `addPendingFile`, and the modal's one-directional `pendingAddFile` → `setImageFile` sync effect (which never clears on `null`) attaches it to whatever modal is now open — so an in-flight analyze of `jeans.jpg` can put the jeans photo on an OPEN EDIT modal for a coat and overwrite the coat's photo on save. **(b)** "Skip photo · Enter manually" carries no `disabled={isAnalyzing}` and does not abort, so a late infer fires `setAddInferred` and the `initialItem` sync effect OVERWRITES fields the friend already typed. **(c)** `onAnalyze`'s `finally` guards the ref with `if (!controller.signal.aborted)` but runs `setIsAnalyzing(false)` OUTSIDE that guard, so a superseded analyze clears the spinner for the one still in flight. (2026-07-25 round-2 deep read.) | **OPEN** | *(dormant until the W-track CV service lands.)* Fix all three in the W-track CV unit, not before: abort in every teardown path (`onClose`/`onEdit`/unmount), make the `pendingAddFile` sync bidirectional (clear `imageFile` when it goes null in ADD mode), gate the skip action on `isAnalyzing`, and move `setIsAnalyzing(false)` inside the not-aborted guard (or compare `cvAbortRef.current === controller`). |
| H84 | **Client silent-failure / honesty cluster: nine places where a failure is invisible or a promise is not kept.** Ranked: **(1)** `writeJSON`'s `catch {}` (`dashboard/page.tsx:115-122`) makes the F10 envelope persist BEST-EFFORT, while the dropped-response arm (`:884`) promises **"Reload to resume — you won't lose your place."** On a sessionStorage quota failure that is false: the `requestId` is gone, `resumePending` never fires, and a PAID render is orphaned. This is the outlier — every other silent catch in these files is paired with an honest fallback. **(2)** `res.json().catch(() => null)` on a **200** (`:853`) lets `setResult(null)` through, returning the dashboard to its virgin "Click Get Recommendations" state after a completed request. **(3)** `handleLogout`'s catch (`:785`) only logs — a failed sign-out leaves the friend signed in with no message, AFTER both sessionStorage keys were already removed. **(4)** `dismissGuideForever` (`wardrobe/page.tsx`, by symbol) swallows a localStorage throw, so the button's own "forever" promise silently degrades to session-only. **(5)** the three `if (!firebaseUser) return;` guards at the top of `handleDeleteItem`, `handleToggleAvailability` and `handleClearAll` return BEFORE their `confirm()` and set no error, so Delete / Delete-all / the availability toggle are absolute no-ops — unlike `handleAddItem`, which does say "You are not signed in." **(6)** a successful dislike-reason enrich is indistinguishable from never having tried: `statusFor` returns `undefined` again, so "Tell us why?" reappears (`:551-568`). **(7)** `markFeedback`/`markFeedbackClear` address the card by INDEX while the server binding is by object, so a rollback after the list changed clears the wrong card. **(8)** `persistResult`'s state fallback saves the LIVE textarea text against the OLD render, so a restored dashboard echoes an occasion that no longer describes its outfits. **(9)** editing a photo-less item makes the only save action a grey "Save without a photo" with "Add a photo" primary — it reads as though the edit will discard a photo. (2026-07-25 round-2 deep read.) | **OPEN** | *(session-2 lane.)* (1) and (5) are the load-bearing pair: make `writeJSON` return a boolean and soften the dropped-response copy when the persist failed, and give the three signed-out guards the same `setError` the add path already has. (9) is a copy call ("Save changes" when `isEdit`) but note it will redden several `addItemModal.test.tsx` cases that are unknowingly in edit mode — see the `isEdit` note in H85. |
| H85 | **`isEdit`'s second clause makes many ADD-flow tests silently exercise EDIT mode.** `isEdit` is `existingImagePath` OR (`initialItem` AND no `addStep`) (`wardrobe/page.tsx`, by symbol — this cite was already stale one commit after it was written). Through the page the mis-derivation is UNREACHABLE — `modalInitialItem` only returns an object when `editingItem` is set or `addStep === "form"`, and `addStep` is masked to `undefined` whenever `editingItem` is set, so page-side `isEdit ⇔ editingItem != null` and the `""`-vs-`undefined` subCategory choice always matches the verb (POST vs PATCH). The cost is **test fidelity**: ~17 cases in `addItemModal.test.tsx` pass `initialItem` with no `addStep` and no `existingImagePath`, so tests titled as add-flow cases run the EDIT footer. The genuine add-with-photo footer (which has the third "Save & add another" button) is covered by only two of them — and by the real-browser Phase B run, which is what surfaced this. (2026-07-25 round-2 deep read + browser verification.) | **OPEN** | *(test fidelity, no product defect.)* Do NOT "fix" `isEdit`: clause 2 is what makes edit mode true for an item with no photo. Instead give the modal tests an explicit mode fixture (pass `addStep="form"` for add cases, `existingImagePath` for edit cases) so each test's title matches the cell it exercises. Worth doing before any change to the no-photo footer copy (H84(9)), which those tests currently pin in the wrong mode. |
| H86 | **Deliberately NOT built on 2026-07-25, after a first attempt at them caused more defects than it closed.** Each is a real gap; each needs NEW runtime state or NEW control flow, which is what every self-inflicted defect that day came from (6 of 6; zero came from deletions, string changes or tests). They are features, not fixes, and belong in a specced unit of work rather than inline in an audit. **(a) Recovering an unreadable-200 render.** `runRender` now reports it honestly and clears the envelope like any completed request — so a PAID render is still lost, just no longer silently. Recovering it means keeping the envelope and resuming under the same `requestId` (which §C.4's early read-check would replay free), and that needs a retry bound, or a deterministic failure loops forever telling the friend to reload. The bound is the hard part: it must count observed FAILURES, not attempts, because runbook §8/[[H68]] document ~5 concurrent renders queueing 40-50s past the 45s abort — counting mounts would discard the envelope of a friend who merely reloaded while waiting, and the next Generate then BUYS A SECOND RENDER. **(b) Exporter image-ownership gate.** `scripts/exportTrack2Core.cjs` resolves `wardrobeimages` by `_id` with no owner check. With the write side closed (see PATCH_STRING_FIELDS) nothing new can be mis-pointed, and no such row exists today, so this is defense-in-depth against residue rather than a live leak. Needs TWO layers if built: a byte-level owner check AND a per-training-example-slot check, because on a FULL export the rightful owner is also a referrer, so the byte layer legitimately passes while the borrower's row must still be refused. A single-layer version was written and was a fail-open. **(c) Signed-out guards** on `handleDeleteItem`/`handleToggleAvailability`/`handleClearAll`, which `return` bare with no message. Unreachable through the page — "Delete all" is already `disabled={!firebaseUser}` and the auth watcher clears `items` in the same batched commit, so no card exists to click. **(d) `DISLIKE_REASONS` (dashboard) ⊆ `FEEDBACK_REASON_CODES` (`models/OutfitInteraction`)** has no test: a typo 400s every structured "why" while the one-tap dislike still lands, so the corpus degrades to reason-less rejections invisibly. Untestable as-is — the dashboard is a `.tsx` in the jsdom project, which cannot import mongoose; the constant must move to `lib/` first. **(e) Jest's 5000ms default** applies to `beforeEach`/`afterEach` and every `it()`; `testHarnessContract` only enforces the mongod BOOT hook. Ten sequential `--randomize` runs are green, but six CONCURRENT runs fail 16-36 tests across eleven suites on `Exceeded timeout of 5000 ms`. Not an observed-in-practice failure (nobody runs six jest instances), so it buys margin for parallel CI only. *Trap-guard:* `testTimeout` must go at the ROOT of `jest.config.js` — **Jest 29.7 silently ignores a project-level one**; verify with a test that sleeps 7s and must PASS. | **OPEN** | *(each needs a spec, not an inline fix.)* Rule that produced this list, worth keeping: a change that DELETES, edits a STRING, or adds a TEST is safe to make during an audit. A change that adds state or branching is not — register it. |
| H87 | **A rejected Mongo connect promise is cached for the life of the lambda instance, so ONE transient Atlas failure permanently bricks that instance.** `lib/mongodb.ts:31-44` assigns `cached.promise = mongoose.connect(...)` and never clears it on rejection (`cached.conn` is only set on success). After a single failed connect — Atlas M0 is shared, throttled and SRV-addressed, so an SRV blip or a maintenance stepdown inside the driver's default 30s server-selection window suffices — every later `connectMongo()` on that warm instance re-awaits the SAME rejected promise and throws instantly, with no retry, **even after Atlas is healthy again**, until Vercel recycles the instance (which low traffic makes slow). `connectMongo` sits under `lib/apiAuth.ts` and `lib/session.ts`, so the instance loses authentication too — this is not one broken feature but the whole app for whoever routes to it. Found independently by two audit lanes and by direct read. (2026-07-26 deployed-app audit.) | **FIXED: 4baa6c3e** | *(RESOLVED — the canonical cached-connection form LANDED (`lib/mongodb.ts`).)* The await is wrapped: a rejection clears `cached.promise` so the next request redials, while a successful connection stays memoized. Mutation-proven in `tests/mongoConnectRetry.test.ts` (removing the clear reddens the reconnect case). Landed with H88 as the row required. |
| H88 | **A database failure is reported to the user as an expired login.** Both auth helpers wrap `initDatabase()` inside the SAME `try` as token verification and map every throw to 401: `lib/apiAuth.ts:31-40` → `"Invalid or expired token"`, `lib/session.ts:30-38` → `"Invalid or expired session"`. Any Mongo fault (H87's bricked instance, an M0 connection-cap rejection, a slow cold connect) therefore surfaces as an auth failure rather than a 503. The friend is told to sign in again, does so successfully at Firebase, and still fails — because the real fault is the database, and nothing in the UI can say so. The client gates compound it: `RedirectIfAuthenticated.tsx:48` and `AuthGate.tsx:25-41` fire `/api/auth/sync` for its side effect and never check `res.ok`, so a 500 there still admits the user into a fully-rendered app in which every later call returns 404 "User not found". (2026-07-26 deployed-app audit.) | **FIXED: 6e6fd45f** | *(RESOLVED — the failure-class split LANDED in the helpers (4baa6c3e) and was COMPLETED on the wardrobe routes (6e6fd45f), which each carried a byte-identical inline copy of the conflated resolver.)* 401 is reserved for genuine token/cookie rejection; a DB fault past verification returns 503 "trouble reaching your closet" — `lib/apiAuth.ts`, `lib/session.ts`, `verifyUserProd` (`lib/mlRecommend.ts`); the three wardrobe routes now delegate (`getUserIdFromRequest = verifyFirebaseUser` — single home, do NOT reintroduce a local resolver). Client half: AuthGate checks the sync outcome — 5xx/network → an honest "temporary problem, try again" retry screen (children never render into a broken app), 401 → signOut (not a redirect loop), clean → admit. Pinned in `authDbFailure.test.ts` (helpers + per-route delegation) and `authGateDbTrouble.test.tsx`, mutation-proven. Residual split to [[H109]]: the sync route's checkRevoked arm can 401 a transient Firebase-Admin fault, which the new signOut arm acts on. |
| H89 | **The account page renders permanently blank on a load failure, and both its save actions swallow network errors.** `app/(app)/account/page.tsx` uses `try { … } finally { … }` with **no catch** in three places. On load (`:128`) a *network-level* rejection — as distinct from an HTTP error status, which the `if (!res.ok)` arm does handle — leaves `loading=false, error=null, user=null`: a combination that renders nothing at all, a white screen with no message and no retry. In `saveFeedback` (`:198`) and `saveProfile`, a rejected `fetch` yields neither an error nor a confirmation — the button simply flips from "Submitting…" back to "Submit feedback", which reads as success while the friend's typed feedback is gone. Ordinary mobile connectivity (lift, dead zone, wifi handoff) is the trigger. NOT covered by H84, whose nine items are all in `dashboard/page.tsx` / `wardrobe/page.tsx`. (2026-07-26 deployed-app audit.) | **OPEN** | *(friend-facing.)* Add a `catch` to each of the three: set an error string on load so the page can never render empty, and show an explicit failure message on both saves. Purely additive error handling on paths that have none today — no new state machine, so it is not an H86-class feature. |
| H90 | **The dashboard discards the pending-render envelope on ANY non-2xx, including 5xx that land after the render was paid for.** `app/(app)/dashboard/page.tsx:855` guards with `if (!res.ok)` while its own comment claims the branch handles "a stable 4xx/409 … terminal for this action". It also catches the route's 500 arm and every platform 5xx — including a Vercel `FUNCTION_INVOCATION_TIMEOUT` that can fire *after* the OpenAI call completed and even after the Mongo write. `removeKey(PENDING_KEY(userUid))` then deletes the `requestId`, so `resumePending` can never replay it and §C.4's early read-check can never return the already-persisted render for free. The defect is precisely that the code does not do what its comment says. Distinct from H86(a) (the unreadable-**200** arm) and from H84(1)-(2): this is the **5xx** arm. (2026-07-26 deployed-app audit.) | **FIXED: f75b9dd5** | *(RESOLVED — the condition now matches its comment.)* `runRender` keeps the envelope on any 5xx and shows "if that outfit finished, it'll come back" (the no-false-promise copy rule); only a stable 4xx clears it. Deliberately NO retry state was added — H86(a)'s failure-counting bound still gates any resume redesign, and H94 (weather-bucket 409 on replay) stays open. Both arms pinned in `dashboardRenderFailure.test.tsx`; the 500-keeps-envelope case is mutation-proven. |
| H91 | **`clearUserWardrobe` destroys the wardrobe first and can then fail, returning 500 over an already-emptied closet.** `lib/clearWardrobe.ts:31-38` runs `WardrobeItem.deleteMany` to completion, *then* resolves `referencedImageIds(...)`, *then* runs the image `deleteMany` — three un-transacted steps. A fault in step 2 or 3 (an M0 blip/stepdown surviving the driver's retry, or the lambda being killed) leaves every wardrobe item permanently destroyed while `clear/route.ts:35-38` returns 500 "Failed to clear wardrobe" — the friend is told it failed when it irreversibly half-succeeded — and orphaned image bytes stay retrievable by id. (2026-07-26 deployed-app audit.) | **OPEN** | *(honesty + orphan residue.)* Resolve `keepIds` BEFORE destroying items so a partial failure is recoverable, and report partial success honestly rather than as flat failure — the posture the H63 partial-delete work already established. |
| H92 | **The per-user image budget is denominated in pre-base64 bytes while Atlas stores the base64 string, so the cap admits ~1.37× what it bounds.** `MAX_USER_IMAGE_BYTES = 80MB` (`app/api/wardrobe/[id]/image/route.ts:18`) is summed over `WardrobeImage.sizeBytes`, which `lib/imageStorage.ts:65` sets to `input.bytes.length` — the *original* length — while `:56` persists `input.bytes.toString("base64")`, 4/3 the size (`models/WardrobeImage.ts` documents the field as "original bytes length (before base64)"). A user at quota therefore occupies ~107-110MB of the shared 512MB M0, not 80MB. The route's own comment reasons explicitly against that 512MB ceiling, so the intent is right and only the unit is wrong. An M0 at its storage cap fails **writes** cluster-wide — snapshots and feedback, i.e. the Track 2 corpus itself, not merely uploads. Unreachable at today's single-closet scale; reachable with 4-5 photo-heavy closets. (2026-07-26 deployed-app audit.) | **OPEN** | *(capacity, pre-recruit.)* Either charge the budget `Math.ceil(bytes.length * 4 / 3)`, or store `sizeBytes` as the encoded length and rename the field. Re-check real cluster headroom before recruiting past ~4 closets. |
| H93 | **A model comment asserts an index-build mechanism production no longer runs, so the next NEW index will silently not exist in prod.** `models/GenerationSnapshot.ts:632-633` states indexes are "built automatically on first boot via autoIndex … No migration script", but `lib/mongodb.ts:38` sets `autoIndex: process.env.NODE_ENV !== "production"` — off in prod since 2026-07-18 (`414dca7b`). Verified NOT a live defect: before that commit the production connection ran an explicit `autoIndex: true` (`git show 414dca7b^`), and every current index declaration predates it, so the live cluster's indexes — including `User`'s unique email/identity pair and the snapshot's partial-unique `{user, requestId}` idempotency index — were built. The hazard is forward-only: any index added from now on never builds in production, and this comment tells the next implementer no action is required. Tests structurally cannot catch it — the in-memory test Mongo builds indexes automatically, so the suite stays green. (2026-07-26 deployed-app audit.) | **OPEN** | *(latent trap, no live impact.)* Delete the false half of the comment and state the rule `lib/mongodb.ts:36-37` already gets right: "a NEW index requires a one-time manual build in prod." Any future index addition carries a checklist step to run `createIndexes` against Atlas. |
| H94 | **A resumed render re-resolves weather live, so the replay can 409 instead of returning the render already paid for.** The weather bucket is part of the §C.4/G5 render identity (`makeRenderIdentity`, compared at `lib/mlRecommend.ts:402-408`), but on a root render it is derived server-side from a live, fail-open third-party call (`lib/mlRecommend.ts:151`) and the client's F10 resume envelope never freezes it. If the bucket moves between the original request and the resume — real across a temperature-bucket boundary, and guaranteed if the weather call fails open on exactly one of the two — the identity no longer matches and the replay is rejected as a conflict instead of returning the completed render. (2026-07-26 deployed-app audit.) | **OPEN** | *(frequency unconfirmed.)* Freeze the resolved weather bucket into the F10 envelope and replay it, or exclude weather from the identity comparison on a resume. Interacts with H90: the envelope has to survive for there to be anything to replay. |
| H95 | **The daily monitor watches only the Fly half, so every web-side silent failure it names in its own header goes unnoticed.** `scripts/track2-monitor.mjs:5-7` states it exists to poke three things that "fail SILENTLY: a dead service link, a lost single-machine pin, or a mistyped env var". Its two HARD checks (`evaluateChecks`, `:57-62`) consume only `checkReadyz` (`:66-80`, a direct fetch of `${SERVICE_URL}/readyz`) and `checkMachineCount` (`:82-99`). **Nothing in the file touches the Vercel app** — there is no `APP_URL` reference at all — so the third named mode is exactly the uncovered one, and it is web-side: `USE_ML_SHORTLISTER` unset → every friend gets the §A degraded empty state; `ML_SERVICE_URL` mistyped → every render fails; `FITTED_SERVICE_KEY` ≠ the service's `SERVICE_KEY_CURRENT` (the naming footgun CLAUDE.md documents) → every render 401s. The last is the sharpest: **`/readyz` requires no auth** (verified live 2026-07-26 — an unauthenticated `curl` returns `200 {"ready":true,…}`), so a shared-secret mismatch leaves the monitor reporting green while not one render can complete. `yieldReadout` (`:101-120`) would eventually show flat growth but is informational by construction ("never flips the monitor verdict") and only ever lands in a log file. The failure presents to Brian as friends who quietly bounced. (2026-07-26 read-only bug hunt, round 2.) | **OPEN** | *(the pre-recruit monitor's main blind spot.)* Add a third hard check that exercises the WEB half through the same door a friend uses: an authenticated `POST /api/recommend` is too expensive to run daily, so assert the cheap invariants instead — the app root returns 200, and a minted-token `GET /api/wardrobe` returns 200 (proves Vercel + Firebase Admin + Atlas + `USE_ML_SHORTLISTER`'s route are all alive). A key-mismatch check needs a service-side authenticated no-op endpoint; absent one, at minimum stop implying the monitor covers env vars. |
| H96 | **A typo'd `--operatorAuthId` silently disables the prereg §5 operator exclusion, and the CLI prints nothing that would reveal it.** `scripts/export_track2.mjs:70-75` warns only when `operatorAuthId` is **falsy**. `resolveExcludedUsers` (`exportTrack2Core.cjs:227-240`) looks the id up and returns `operatorResolved: false` when it matches no user — the correct detection — but the CLI never surfaces it: line 86 prints `manifest.counts` only, and `operatorResolved` lives under the separate `manifest.exclusions` key. So a mistyped/stale/wrong-project Firebase uid produces a run that looks identical to a correct one while Brian-as-friend-#0's self-labeled closet enters the headline certificate arms — the exact contamination the flag exists to prevent, on the artifact that fires the frozen Look-1 trigger. `arg()` (`:46-49`) widens the mouth: it returns `process.argv[i+1]` unconditionally, so `--operatorAuthId --out ./x` binds the operator id to the literal string `"--out"`, truthy and unresolvable. Verified live 2026-07-26: a scoped export printed the counts block with no exclusion line, while `manifest.exclusions.operatorResolved` was `false`. The `track2test_*` half of the exclusion works correctly (observed excluding a live test persona). (2026-07-26 read-only bug hunt, round 2.) | **FIXED: 9f433d95** | *(RESOLVED — fail-loud LANDED.)* `exportTrack2` resolves the §5 exclusions BEFORE any filesystem write and THROWS when a supplied operator id matches no user (the CLI exits 1 — an unresolvable id is an operator error, as this row ruled); the CLI prints the exclusion line next to the counts on success and a loud NOT-a-real-read warning when no operator id was given, so absence is detectable; and `cliArg`/`cliArgAll` (moved into `exportTrack2Core.cjs` so jest exercises the real unit) refuse a flag whose "value" is another flag, closing the `arg()` widening. Prereg files untouched (check 11 green); eligibility semantics unchanged. Mutation-proven in `exportTrack2.test.ts`. |
| H97 | **The one §12 repair retry re-sends a byte-identical ask, so a truncation-caused parse failure buys a second call that cannot succeed.** `_repair_prompt` (`fitted_core/rescue.py:878-886`) rebuilds the prompt with `replace(prompt, system=prompt.system + _REPAIR_INSTRUCTION)` and its own docstring confirms "`user` and `candidate_requested` are untouched". `_REPAIR_INSTRUCTION` (`:738-741`) asserts "Your previous output was not valid JSON" and asks for strict JSON. That is the right cure for a formatting failure and the wrong one for the **truncation** failure: when the output hit `max_completion_tokens`, the JSON was well-formed and merely cut off, so re-asking for the same N outfits under the same cap truncates again deterministically — a second paid call, ~2× latency, and the same "couldn't find enough" fallback. This is reachable precisely where `service/config.py:64-77` already grades the residual (TOKCAP-2): `_rescue_candidate_requested` clamps to `[MIN_RESCUE_CANDIDATES=6, MAX_CANDIDATES=40]` (`rescue.py:472`), so a high-yield rescue out-asks the 12-outfit worst case the 2200 cap was validated against. The diagnosis is **already in hand and unused**: `_generate_and_parse_with_trace` records attempt 0's `finish_status` at `:1147` — before the retry at `:1152` — and retries regardless. Distinct from TOKCAP-2, which is about the cap's size; this is about the retry being unconditional. (2026-07-26 read-only bug hunt, round 2.) | **OPEN** | *(wasted spend, honest failure.)* Branch on the captured `finish_status`: on an abnormal/length finish, either skip the retry outright (the fallback is reached one call sooner and cheaper) or retry with a **reduced** `candidate_requested` and a truthful instruction ("return fewer outfits"), which is the only variant that can actually succeed. Keep the blind JSON repair for genuine format failures. |
| H98 | **Three parsers of the same image reference disagree, and the exporter's rejects a form the other two accept — reporting a photographed item as photo-less.** The UI helper `lib/imageUrl.ts resolveImageSrc` accepts three forms: `mongo:<id>`, `/api/images/<id>`, and `http(s)://…` (the §15.2 external passthrough). The render adapter `lib/mlRequestAdapter.ts:145-151` **prefers `item.imageUrl`** and passes any string through verbatim, so an `http(s)://…` reaches `itemSnapshots[].engineVisible.imageUrl` (`:266`). But the exporter's `parseImageId` (`scripts/exportTrack2Core.cjs:77-83`) handles only the first two and returns `null` for an absolute URL. The consequence is not a missing file but a **wrong diagnosis plus silent decidability loss**: the slot lands as `imageStatus: "none"`, which `manifest.counts.itemSlotsWithoutImageRef` (`:376-385`) documents as "an item saved photo-less, whether deliberately or because its upload failed", and because `imageUsable` requires EVERY item to resolve, one such item disqualifies every outfit containing it from BOTH scoreable arms of the frozen certificate — a corpus reporting a clean bill of health while shedding the sample the M6 read needs. **Latent, not live:** `WardrobeItem.imageUrl` exists (`models/WardrobeItem.ts:29`) but no route writes it — it is absent from the create allowlist and was removed from `PATCH_STRING_FIELDS` with `imagePath` (H78) — and `CV_SERVICE_URL` is unset in production. It arms the moment the W-track CV service writes `imageUrl` (§18), which is the stated plan. (2026-07-26 read-only bug hunt, round 2; the exporter's join was live-verified otherwise clean — 8/8 images resolved, 0 unmatched, 0 orphan labels on a fresh persona.) | **OPEN** | *(latent, arms with the W-track.)* Single-home the reference grammar: one parser, imported by all three (the UI helper is already the most complete). Whatever the shape, `parseImageId` must distinguish "no reference" from "a reference in a form I don't handle" — the second is a bug report, not a photo-less item, and must not read as `"none"`. Fix before the W-track writes its first `imageUrl`, not after. |
| H99 | **The erasure gate's comment claims a settle it does not perform.** `scripts/track2-erasure-check.mjs:61-62` reads `// brief settle for the phase-3 sweep` immediately followed by `const after = await countsFor(db, userId)` — there is no delay. Harmless **today**, and verified so: `DELETE /api/account` awaits all three erasure phases before responding (`app/api/account/route.ts` — redact, `deleteUserWithData`, then `cascadeDeleteUserData`), so the counts are genuinely final when read. The hazard is forward-only and is the H90/H93 family: the comment tells a future maintainer a settle already exists, so moving the idempotent phase-3 sweep off the response path — a plausible latency optimization — would make the gate read too early and report a FALSE FAILURE on a correct erasure, discrediting the one artifact that proves the deletion promise. (2026-07-26 read-only bug hunt, round 2.) | **OPEN** | *(comment-only, latent.)* Delete the false comment, or make it true with an explicit short wait plus a bounded re-read. Do not leave a claim of synchronization the code does not implement. |
| H100 | **Regenerate locks did not survive a re-roll** (friend-reported 2026-07-27 as "regenerate is not locking the item that was locked in the original prompt"): `RegenerateModal` is conditionally mounted and seeded `locked`/`disliked` from empty sets on every open. **Fixed — locks are now sticky across the re-roll lineage, client-remembered:** the dashboard remembers the `lockedItemIds` last submitted on an accepted re-roll (`activeLocksRef`, `app/(app)/dashboard/page.tsx` — set in `submitRegenerate`'s accept path and on a resumed re-roll's envelope, persisted in the sessionStorage dashboard blob for reloads) and seeds the next modal with them, **intersected with the displayed outfit's items** (the modal only renders toggles for what is on screen; a lock for an absent item would be invisible, un-clearable state — locks are a hard constraint per `fitted_core/ranker.py:362`, so the intersection normally drops nothing). A fresh root render resets the lineage (the §C.3 root-controls invariant is untouched and test-asserted). **Avoids are deliberately one-shot:** the engine excludes an avoided item from every child candidate, so an inherited avoid could never be displayed or cleared in the modal, and silent accumulation would starve a small closet (a live friend closet has 6 items) into "nothing buildable" with no visible cause; the durable "stop showing me this" signal is the Dislike feedback, not the per-roll steering control. Behavioral suite: `tests/dashboardRegenControls.test.tsx` (real component, mutation-checked RED). **Trap-guards that remain true:** (1) do not "fix" the rescue *anchor* — `lib/mlRecommend.ts:332` derives `forcedItemId` from the parent, never the client, verified on live data (Marc's re-roll kept the forced athletic short in all six children); (2) the register's old suggestion "seed the modal from the parent snapshot's stored `controls`" is **not client-reachable** — the row stores `controls` but the §A/G15 browser projection (`lib/mlSnapshotMerge.ts projectBrowserResponse`) never returns them; the client can only remember what it itself submitted, which is what the fix does. Watch-item (n=1, explained, not chased): a live child `c0` was item-identical to the parent's `c0` — consistent with regenerate being constrained *fresh* generation with no dedup against the parent's combos (`parentCandidateCacheKey` is only a payload cross-check), so a small closet under the same lens can reproduce a combo by chance. | **FIXED: f4528567** | *(IMPLEMENTED 2026-08-23 — `app/(app)/dashboard/page.tsx` (`activeLocksRef` + `RegenerateModal initialLockedItemIds`), tests `tests/dashboardRegenControls.test.tsx`..)* |
| H104 | **The export certificate can exclude only ONE operator account, so a second personal account counts as a friend.** `resolveExcludedUsers` (`fitted/scripts/exportTrack2Core.cjs:227`) accepts a single `operatorAuthId` plus the `track2test_*` regex. Brian's second personal account `brian.li.fmlbkup@gmail.com` (`authId HftfgRJg7fXeXhmZxrSNgdvaFei1`) matches neither, so it lands in the headline pool: today's certificate reports `"friends": 3` when only two are real testers, and contributes 1 accepted / 2 rejected scoreable clusters to a read whose decision floors are 25 and 25. Prereg §5 author-exclusion integrity. Distinct from **DEFECTS-H96**, which covers an *unresolvable* operator id rather than a second valid one. (2026-07-27 export read.) | **FIXED: 9f433d95** | *(RESOLVED — multi-account exclusion LANDED.)* `--operatorAuthId` is repeatable and comma-listable (env fallback included); `resolveExcludedUsers` normalizes to a list, reports per-id resolution, and quarantines EVERY resolved operator closet into `yield.excluded`; the single-string form keeps working and `manifest.exclusions` now carries `operatorAuthIds` + `operatorResolution`. Runbook §8's export command now says: pass EVERY personal account that currently exists (the `fmlbkup` backup was found erased at the 2026-08-23 live verify — no user row, its rows already cascade-deleted — so only the main uid is passed until a second personal account exists again; a stale id now refuses the run per H96). End-to-end two-operator case + comma-list resolution mutation-proven in `exportTrack2.test.ts`; live-verified: exclusion line printed, `friends: 2`. |
| H107 | **H77's residual siblings — three small wardrobe-surface defects, deferred by decision 2026-07-25.** **Residual siblings (still OPEN, deferred by decision):** edit hardcodes `size:""/notes:""` clearing those fields on any row that has them (no UI field today); a since-deleted-snapshot history card gives a retry-forever "try again" on Flip (`history/page.tsx:56-62`); `handleAddItem`'s "saved but server returned no id" path still returns an error STRING after a 201 (same duplicate-mint class as (a); near-unreachable — Mongo always returns `_id`). | **OPEN** | Split from [[H77]] (D6c: one row, one status). Pick up with the owning surface; each adds state or branching, so per the [[H86]] rule it belongs in a specced unit of work, not an inline audit fix. |
| H108 | **The M6 exporter resolves `wardrobeimages` by `_id` with no owner check — [[H78]]'s read side.** With the write side fixed, no route can mis-point `imagePath` at a foreign image, so this is defense-in-depth against residue rather than a live leak. The full analysis is [[H86]](b): it needs TWO layers if built — a byte-level owner check AND a per-training-example-slot check, because on a FULL export the rightful owner is also a referrer, so the byte layer legitimately passes while the borrower's row must still be refused (a single-layer version was written once and was a fail-open). Also extend `exportTrack2.test.ts`'s userFilter-scoping test to `wardrobeimages` — it covers the three row collections and skips the one carrying actual photo bytes. | **OPEN** | Split from [[H78]] (D6c: one row, one status). Build with [[H86]](b) when the exporter is next touched — two layers or nothing. |
| H109 | **The sync route's checkRevoked arm maps ANY throw to 401, and AuthGate now signs the user out on a sync-401 — so a transient Firebase-Admin fault during a FIRST-ever sign-in reads as a rejected login.** `app/api/auth/sync/route.ts` re-verifies with `checkRevoked` on the create path only (no Mongo row yet); its catch returns 401 "Invalid or expired token" for a revoked token AND for a transport/network fault alike. Since the H88 fix, AuthGate treats sync-401 as a credential rejection and signs out. Blast radius is narrow: warm-path users never pay the checkRevoked round-trip, and the signed-out user recovers by signing in again. (2026-08-23 fresh-context review of the H87/H88 fix batch.) | **OPEN** | *(narrow, first-sign-in only.)* Distinguish the two classes in the checkRevoked catch: an admin-SDK `auth/id-token-revoked` (or equivalent code) → 401; any other throw → 503, so AuthGate shows the retry screen instead of signing out. Verify the SDK error codes at fix time rather than trusting this row. |
