# H26 spike runner. Run from this directory: `pytest`. rootdir is pinned here so the
# spike's tests stay isolated from the fitted_core suite (ml-system/pytest.ini pins that
# one to its own tests/). See docs/plans/h26-compatibility-spike-v2.md §15.
[pytest]
testpaths = tests
