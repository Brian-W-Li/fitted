# Core (outfit_recommender.py)
# No extra deps for rule-based recommender

# fitted_core/ substrate (M0–M2) — pure functions + tests
pytest>=7.4

# CV / clothing attribute inference (clothing_cv.py)
numpy>=1.20
Pillow>=9.0
torch>=2.0
transformers>=4.30
rembg[cpu]>=2.0
